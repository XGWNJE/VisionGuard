# Verification Report

本文是 VisionGuard 的验证证据台账，不是产品路线图，也不把源码存在、编译成功、帧循环、FPS、界面启动或截图展示写成完整功能通过。每条结论都注明验证范围；详细命令由[运维文档](60-operations.md)和两个保留 Skill 维护。

## 判定词

- **已自动验证**：有可复现命令和机器可读结果，断言覆盖了所写语义。
- **主体实现**：源码已具备功能，但验证范围不足以宣布完整交付。
- **待人工目检**：需要 owner 检查真实 UI、窗口采集或发行包表现。
- **待真机**：需要授权的真实 Android 设备或目标硬件。
- **未实现/未来规划**：当前源码没有该能力，不能用局部 smoke 代替。

## 当前仓库证据

| 范围 | 状态 | 本次证据能证明什么 | 证据 |
|---|---|---|---|
| 文档与治理审核 | 已自动验证（本次运行） | Markdown 链接/锚点、编码、版本、组件、入口、Skill 和产品边界符合契约 | `node scripts/check-docs.js`；本文件所在提交后的命令输出 |
| Server 单测与协议测试 | 已自动验证（本次运行） | 14 项测试通过；覆盖通道不匹配拒绝、报警落盘后 ACK、重复重试不广播、同 ID 冲突拒绝、告警/截图安全、控制请求关联，以及 2026-09-15 新增的下发字段与语义：`device-list` 携带 `maxSources`/`sourceLimitExceeded`、超限心跳整组拒绝后保留旧来源快照并由下一个正常心跳清除标记、驻留-only 设备的业务命令回“该设备当前没有检测端在线”。设备级命令仍按整机中继，由检测端解释为全部来源 | `npm --prefix server test`；`server/dist/index.js` |
| 新协议隔离测试通道 | 已自动验证（本次运行） | 独立 Server 进程使用临时端口和独立数据目录启动，`/health` 返回指定 `vnext-runtime-smoke` 通道；协议测试证明其他通道认证被拒绝 | `scripts/start-isolated-test-server.ps1`；本次本地运行输出；`server/test/control-request.test.ts` |
| Windows 报警可靠发件箱 | 已自动验证（单元/构建） | 检测端队列跨实例保留、同 ID 替换、ACK 后持久删除、损坏文件隔离均通过；net472 检测端与 WinForms Release 编译通过（WinForms 端已随 V10 退役，该半条证据不再适用于当前代码）。尚未执行真实断网重连和跨进程截图链 | 检测端队列断言原由 `tests/WindowsConfig.Tests`（**该工程已随 WinForms 退役删除，当前无等价入口**）覆盖；历史命令 `visionguard-build -Target WinForms` 已无对应工程 |
| Android 接收端控制完成语义 | 已自动验证（单元/构建） | 只把字段完整的 `completed` 回执映射为结构化结果，缺失阶段的旧载荷被拒绝；冷却档位保留设备当前值（不在预设档位时并入选项，未修改不产生变更）、缺少 `maxSources`/`sourceLimitExceeded` 的旧记录按默认值解析；Debug 单测与 APK 构建通过 | `receiver/android/gradlew.bat testDebugUnitTest assembleDebug` |
| WPF → 隔离 Server → Android 接收端报警链 | 已自动验证（确定性传输链） | `WpfAlertChain.Probe` 复用生产 WPF `ServerPushService` 发送 `person` 报警与真实 PNG；Server 完成落盘、ACK、广播和截图备份，Android API 36 模拟器认证隔离通道、收到报警并缓存同一 `alertId` 截图，界面可检索到探针报警。修复发件箱按通道分区后复测只新增一条记录。该测试与同轮四窗口人员推理分别通过，但尚未把推理触发与传输探针合成一个进程内测试 | `artifacts/e2e/20260912-234853/summary.json`；`tests/WpfAlertChain.Probe/`；隔离数据 `.local/e2e-server/vnext-e2e/alerts.json`；模拟器日志中的 `6c5dc71a-7c83-4cb5-ae57-0ff82ee89c35` |
| ServerBuild | 已自动验证 | TypeScript 编译成功且 `server/dist/index.js` 存在；不是 HTTP/WS 运行 E2E | `artifacts/e2e/20260910-063530/summary.json`、`server-build.txt` |
| WPF 四路真实窗口人员推理 | 已自动验证（100% 与 200%） | 200% 缩放下以 DPI-unaware net472 窗口工具承载 6 个独立窗口，候选客户区为 1280×720 而目标自绘为 640×360；规范化后 WPF DirectML 每路约 3 FPS 且每帧命中 `person`（60/60、87/87、92/92、98/98、103/103、70/70）。无黑屏或非预期采集错误，窗口子区域裁剪、移动缩放、遮挡、最小化恢复、关闭隔离和停止/重配隔离全部通过；100% 同链路回归也通过 | `artifacts/e2e/wpf-window-person-detection-client-plane-200pct.json`；`artifacts/e2e/wpf-window-person-detection-client-plane-pass.json`；`visionguard-build -Target Windows` |
| WPF 三路人员图片推理 | 已自动验证（历史记录） | 三张含人图片每路至少一帧 `person`，并通过停止隔离、配置隔离和 DirectML 失败回退；该轮断言的是当时生效的“CPU 多路拒绝”边界，该边界已于 2026-09-13 撤销（见路线图 8.12） | `artifacts/e2e/20260910-063535/summary.json`、`artifacts/e2e/20260910-063535/wpf-person-detection.json` |
| WPF DirectML/CPU 负样本对照 | 已自动验证（有限范围） | 当前界面截图样本在两后端均无有效检测；不证明人员召回或代表性精度 | `artifacts/v3/wpf-directml-cpu-parity.json` |
| WPF 现代化主界面 | 已自动验证（100% 与 200%）/待 owner 目检 | 100% 下 Release 程序及三个控制台页面的运行、交互与布局验证通过。2026-09-15 在 200% 首次复验时曾因自定义 DPI 基线初始化和模态框重入持续弹出“调度程序处理已暂停，但仍在处理消息”；删除启动 DPI 基线、停止/重启策略并修复窗口关闭递归后，重新以 owner 可见的真实 Release 主程序验证：只有一个 `VisionGuard` 顶层窗口，切换“运行环境/当前来源”后仍响应，正常关闭在 10 秒内退出，应用日志无 `.NET Runtime`、Application Error 或 WER 事件。2026-09-16 再次复核：顶部重复摘要与本机全局启停已移除，来源卡片标题栏各自提供新增、删除图标，长状态会截断并保留完整悬浮提示 | `artifacts/e2e/20260915-225349/wpf-real-window-200pct.png`；历史 100% 证据 `artifacts/e2e/wpf-density-source.png`、`artifacts/e2e/wpf-density-environment.png`、`artifacts/e2e/wpf-density-connection-final.png`；`visionguard-build -Target WPF` |
| 来源术语与设置键迁移 | 已自动验证（单元 + 两端真实启动） | 三端统一使用“来源”：检测端卡片与标签页显示“来源 N”“当前来源”，新增/删除入口位于每张来源卡片标题栏（只有一个来源时删除不可用），默认 1 个来源；旧设置键 `Signal.*` 一次性迁移到 `Source.*` 且旧键保留作备份，迁移的补写、幂等与数量键优先级由 `WindowsConfig.Tests` 断言（**该工程已随 WinForms 退役删除，这条单元证据当前无等价入口**）；用旧键种子启动 WinForms 隔离配置，确认来源名、来源身份与冷却值全部迁移且旧键保留（WinForms 端已退役，该半条证据不再适用于当前代码）。`sourceId` 取值未改，避免接收端历史与静音偏好换主。来源数量另经交互验证：对未声明上限的服务端连续点“新增来源”，UI 计数从 1 增至 9 并落盘 `Source.Indexes`，不再卡在 4 路（此前 WPF 会把缺失的 `maxSources` 当成默认 4 路）| `artifacts/e2e/verify-source-settings-migration.ps1`；`artifacts/e2e/verify-wpf-source-count.ps1`；`artifacts/e2e/wpf-source-naming.png` |
| WPF 采集尺寸与遮罩窗口约束 | 已自动验证（边界、运行枚举与实际拖拽）/待人工目检（遮罩布局） | 统一断言宽高分别严格大于 100 物理像素：`100×101`、`101×100` 和 `99×500` 均拒绝，`101×101` 接受；200% DPI 映射断言覆盖 `50 DIP → 100 px` 拒绝和 `50.5 DIP → 101 px` 接受。Win32 创建并调整为 `100×150` 物理像素的可见测试窗口未出现在实际枚举结果中；在 Release 程序中用系统鼠标实际拖出 `93×93 px` 屏幕选区后，选择器保持打开并显示“选区过小”，未接受配置。窗口选择确认、区域选择、配置就绪、启动、屏幕截图和窗口截图均复用相同边界；遮罩窗口声明 520×360 逻辑像素最小尺寸并在构造时钳制 | `visionguard-build -Target WPF`；Windows UI Automation + Win32 鼠标输入；`CaptureSizeConstraints` DPI 映射及边界断言（原驱动工程 `tests/WindowsConfig.Tests` **已随 WinForms 退役删除，当前无等价入口**）；`MaskEditorWindow.xaml` 最小尺寸 |
| V10 Windows 检测端统一 · 批次①（net472 迁移） | 已自动验证（构建 + 真实窗口启动） | `detector/windows-wpf` 目标框架由 `net9.0-windows` 改为 `net472`，新增显式入口 `Program.cs`（设置 TLS 1.2）与 `Runtime/IsExternalInit.cs`、`Runtime/Net472Compat.cs`；修复 36 处 API 差异（`Math.Clamp`、`string.Split` 的 `TrimEntries`/char 重载、`string.Join(char,…)`、`File.Move` 三参重载、`File.WriteAllBytesAsync`/`WriteAllTextAsync`、`ZipFile.ExtractToDirectory` 的 overwrite 重载、`Environment.ProcessId`、`frames[^1]`）。5 个依赖工程（`WpfAlertChain.Probe`、`WpfInference.Benchmark`、`windows-wpf-smoke`、`SingleInstance.Probe`、`WindowsConfig.Tests`）同步迁移到 net472 并各自构建通过（其中 `windows-wpf-smoke` 之外的 WinForms 侧工程与 `WindowsConfig.Tests` 已随后退役删除，现存探针见[运维文档](60-operations.md)）；夹具工程补齐 `GetValueOrDefault`/`Contains(StringComparison)`/`^1` 等兼容替换。**已验证**：Release 构建 0 错误；真实 Release 窗口启动，标题 `VisionGuard`、内存约 112 MB、程序集 `Amd64`/4.4.4.0 后正常关闭。**未验证（属后续批次）**：双推理档位与原生库绝对路径预加载、自研 WebSocket 客户端、驻留单次启动、Win7 实机验收 | `dotnet build detector/windows-wpf/VisionGuard.csproj -c Release`；`detector/windows-wpf/bin/x64/VisionGuard.exe`；各依赖工程 `dotnet build`；真实窗口启动记录 |
| V10 Windows 检测端统一 · 批次②（双推理档位） | 已自动验证（两档构建 + 真实窗口与原生库路径取证） | 同一份源码按 `OrtProfile` 构建两档，各自锁定同代托管/原生配对：`modern`=托管 ORT 1.19.0 + 原生 1.19.0 + DirectML + 模型 YOLO26；`legacy`=托管 ORT 1.2.0 + 原生 1.1.0 + 固定 CPU + 模型 YOLOv5。原生库不再留在应用根目录，构建后布置到 `native\legacy` 与 `native\modern`，由 `Runtime/NativeLibrarySelector.Initialize()` 用**绝对路径预加载**选择档位（`PATH` 会被系统目录抢占，已实测）。**已验证**：两档 Release 构建各 0 警告 0 错误；真实进程模块枚举确认 legacy 档加载 `bin\x64\legacy\native\legacy\onnxruntime.dll`、modern 档加载 `bin\x64\modern\native\modern\onnxruntime.dll`，两档应用根目录均无 ORT/DirectML 原生库；UI Automation 读取“运行环境”页确认 legacy 档模型下拉为 `YOLOv5su 320`、后端下拉为 `CPU · Windows 7 固定后端`，modern 档为 `YOLO26s 320` 与 `DirectML · GPU 加速`。**未验证**：Win7 上 legacy 档的实际推理（探针阶段已在 Win7 实机验证同配对可跑通 `yolov5nu_320`，正式源码待随包验收）、多路帧率 | `dotnet build detector/windows-wpf/VisionGuard.csproj -c Release`（modern）与 `-p:OrtProfile=legacy`（legacy）；`visionguard-build -Target WPF`；`detector/windows-wpf/bin/x64/{modern,legacy}/`；进程模块枚举与 UI Automation 输出 |
| V10 Windows 检测端统一 · 批次③（自研 WebSocket 客户端） | 已自动验证（真实服务器认证 + 长连接） | 新增 `Net/MinimalWebSocketClient.cs`，继承 `System.Net.WebSockets.WebSocket` 抽象类，对上层与 `ClientWebSocket` 等价；显式 `SslProtocols.Tls12`、HTTP 升级与 `Sec-WebSocket-Accept` 校验、掩码帧读写、ping→pong、close 处理。移除 `websocket-sharp` 依赖（产物中已无该 DLL）。**已验证**：modern 档连接生产 Server 并持续运行 70 秒无断开；legacy 档（本机强制 legacy）UI Automation 读到连接页 `● 已连接`、`当前版本 4.4.4`；两档构建 0 警告 0 错误。**未验证**：Win7 上的实际连接（探针 v8 已在 Win7 实机验证同实现可连通并稳定 62 秒，正式源码待随包验收）、断网恢复与重连退避 | `dotnet build detector/windows-wpf/VisionGuard.csproj -c Release`；真实 Release 窗口 70 秒运行；UI Automation 连接页文本；`detector/windows-wpf/bin/x64/{modern,legacy}/VisionGuard.exe` |
| V10 Windows 检测端统一 · 端到端回归（WinForms 时期的历史记录） | 部分通过（13/14 项检查），唯一未通过项已于 2026-09-17 查明为**夹具缺陷而非产品缺陷** | net472 迁移与双档位改造后重跑 WPF 多路人员推理烟测（2 路真实 `WindowHandle`，net472 夹具承载）：**逐路人员命中与帧率达基线** —— `default` 56 帧命中、2.91 FPS，`signal-2` 43 帧命中、2.90 FPS；`stopOneSourceIsolation`、`configChangeIsolation`、`moveResize`、`minimizeFaultIsolated`、`restoreRecovery`、`occlusionCapture`、`closeIsolation`、`subRegionCapture`、`cpuMultiSourceAllowedWithWarning`、`directMlFailureFallsBackToCpu` 全部通过，`unexpectedRuntimeErrors` 为空。**当时唯一未通过**：`directMlRuntimeFailureIsolated`（烟测自带的 DirectML 注入故障断言，期望逐路 `Error` 包含 `injected DirectML runtime failure`）。**2026-09-17 结论：该判定是夹具窗口未真正显示所致**——夹具首窗体客户区为 0×0，`WindowEnumerator` 按“可见且非最小化、尺寸有效”过滤后拒绝句柄，故障注入用例因此拿不到预期错误。产品侧的故障注入与逐路隔离逻辑没有被改动，也未发现缺陷。详见下一行的复验记录 | `artifacts/e2e/v10-wpf-smoke.json`；驱动入口 `scripts/test-wpf-person-detection.ps1` 与夹具 `detector/windows-winforms-smoke/` **均已随 WinForms 退役删除，当前无等价入口**，仅保留这条历史记录 |
| V10 · WPF 多路人员推理烟测复验（2/3/4 路真实窗口） | 已自动验证（全项通过） | WinForms 退役后 `visionguard-e2e -Mode WpfPersonDetection` 在自身进程内用 `System.Windows.Forms` 打开真实可见顶层窗口（每个显示一张含人图片），逐路 `WindowHandle` 采集后跑 modern 档真实推理。**2 路**：`passed=true`、逐路 2.91 FPS；**3 路**：`passed=true`，`default` 58 帧全命中、`signal-2` 84 帧中 44 帧命中、`signal-3` 69 帧全命中，逐路 2.90–2.91 FPS；**4 路（回归基线）**：`passed=true`、逐路 2.91 FPS。三轮均 `unexpectedRuntimeErrors` 为空、`ActiveBackend=DirectML`、11 项断言（含此前失败的 `directMlRuntimeFailureIsolated`）全部为真。**同轮修掉两个烟测自身缺陷**：（1）夹具首个窗体在宿主最小化启动时可能继承最小化状态、客户区 0×0，被 `WindowEnumerator` 的「可见+非最小化+尺寸有效」过滤后导致进程崩溃——夹具已加显式 `WindowState=Normal` 与客户区校验；（2）故障注入用例原先固定使用 `windows[0]`/`windows[1]`，而 `closeIsolation` 会先关掉 `windows[1]`，使 2 路时该项必然误判为 false（历史 2 路报告 `artifacts/e2e/v10-wpf-smoke.json` 正是如此，4/6 路历史报告为 true），现改为使用仍然打开的窗口、并在 2 路时给两个来源显式不同身份。**这两处都是验证工具的缺陷，不是检测端缺陷**，检测端的故障注入与逐路隔离逻辑未改动。**未覆盖**：6 路（默认夹具目录只有 4 张人像图，脚本按设计报错而非降低来源数）、动态视频、其他 GPU、显卡驱动重置、持续运行、报警链与 UI 目检 | `visionguard-e2e -Mode WpfPersonDetection -WpfSourceCount {2,3,4}`（夹具实现于 `.agents/skills/visionguard-e2e/scripts/e2e-smoke.ps1`）；逐轮证据 `artifacts/e2e/20260917-040252/wpf-person-detection.txt`、`artifacts/e2e/20260917-040339/wpf-person-detection.txt`、`artifacts/e2e/20260917-040428/wpf-person-detection.txt`（各含完整报告 JSON）与汇总 `artifacts/e2e/20260917-v10-wpf-smoke-verify.txt`；固定报告路径 `artifacts/e2e/wpf-window-person-detection.json` 会被下一轮覆盖 |
| V10 Windows 检测端统一 · 批次④（驻留单次启动与统一命名） | 已自动验证（隔离 Server 全链路 + 真实进程行为） | **角色反转**：检测端启动时探测 `Local\VisionGuard.Resident.SingleInstance`，未运行则用 `UseShellExecute = true` 拉起 `VisionGuard.Resident.exe`（写入 `%LOCALAPPDATA%\VisionGuard\resident-config.json`，含 serverUrl/apiKey/deviceId/channel/detectorPath/appId），使驻留脱离父进程生命周期；驻留被拉起时自动登记登录自启。驻留的 WebSocket 也换成与检测端同一份自研 `Net/MinimalWebSocketClient.cs`（源码链接复用，显式 TLS 1.2），移除 `websocket-sharp`。**统一命名**：生命周期命令 `open-detector`/`close-detector`，组件 `detectorApp`，配置键 `DetectorPath`，应用标识 `Detector`，已贯通驻留、`ConnectionManager`、接收端模型与 UI（单一「打开/关闭检测端」入口）、`check-docs.js` 断言与运维/专题文档。**已验证**：驻留与检测端均构建成功；检测端启动后驻留被自动拉起（1 个进程，34 MB）；登录自启注册写入 `HKCU\...\Run`；杀掉检测端（模拟崩溃）后驻留继续存活并保持心跳；在同一隔离 Server 上由接收端角色发送 `open-detector` 后检测端被重新拉起，回执 `phase=forwarded success=true`；Server 14 项测试通过；接收端 `testDebugUnitTest` 与 `assembleDebug` 通过。**未验证**：生产 Server 当前返回 `auth-result success=false reason=invalid role`，即线上尚未部署含独立驻留身份的版本，因此 `windows-resident` 角色在公网不可用——这是部署缺口，不是客户端缺陷；Win7 实机上的整条链路待随包验收 | `visionguard-build -Target Windows`；隔离 Server（`node server/dist/index.js`，PORT=3100，channel=vnext-e2e）+ 真实 Release 检测端与驻留进程；`%LOCALAPPDATA%\VisionGuard\resident.log`；`HKCU\Software\Microsoft\Windows\CurrentVersion\Run`；`npm --prefix server test`；`receiver/android/gradlew.bat testDebugUnitTest assembleDebug` |
| WPF 窗口重绑与故障隔离 | 已自动验证（Windows 单侧）/待真实 GPU 设备故障 | 窗口恢复规则覆盖精确身份命中、唯一稳定身份下标题变化重绑、同身份多候选拒绝和旧配置同名窗口拒绝；实际纯黑 WPF 窗口产生明确黑屏异常。四窗口运行测试覆盖移动缩放、遮挡、最小化故障隔离及恢复、关闭隔离；受控 DirectML 推理引擎在真实捕获/预处理定时链路中抛错后，故障保留在对应来源、其余来源继续运行（此前的“四路统一停机”边界已于 2026-09-14 撤销）。尚未模拟显卡驱动重置或物理设备丢失 | `artifacts/e2e/20260914-113036/wpf-person-detection.json`；窗口重绑断言原由 `tests/WindowsConfig.Tests`（**该工程已随 WinForms 退役删除，当前无等价入口**）覆盖 |
| Windows 驻留程序 | 已自动验证（框架、构建与隔离链路）/待 Win7 实机 | 驻留已迁移到 .NET Framework 4.7.2 x64；维护脚本构建为 0 警告/0 错误，进程命令返回码可测。独立 `resident-win7-e2e` 通道验证驻留认证、组件心跳、Server 生命周期命令转发及带 `requestId` 的完成回执；测试使用不存在的检测端路径断言失败原因。Win7 SP1 x64 上的实际启动、WSS 证书链、登录重启和网络恢复仍待目标系统。**2026-09-16：V10 将把驻留改为由检测端拉起并自行存活（路线图决策 28），命令统一为 `open-detector`/`close-detector`；本条待该批实施后更新。** | `visionguard-build -Target WindowsResident`；`scripts/test-windows-resident.js`；`detector/windows-resident/bin/Release/net472/`；隔离 Server 运行日志 |
| Win7 SP1 x64 前置环境 | 已自动验证 | 目标系统确认为 Windows 7 SP1 x64；KB4490628、KB4474419、KB3140245、WinHTTP 32/64 位 TLS 1.2（值均为 2560）、KB4019990 与 .NET Framework 4.7.2（Release 461814）全部通过机器判定，应用运行验收前置条件已满足 | `scripts/check-win7-prerequisites.ps1`；`artifacts/e2e/win7-prerequisites.json`（2026-09-14T04:27:59Z） |
| WinForms 多来源主体（已退役的历史记录） | 历史记录：主体曾自动验证（100% 与 200%）/待 Win7 实机 | WinForms 检测端已随 V10 退役，以下为当时的证据：源码 Release 构建通过；200% 缩放下 DPI-unaware net472 夹具的 6 路 YOLOv5 CPU 推理每路均命中 `person`、无黑屏错误，约 2.91–2.94 FPS，窗口子区域裁剪尺寸断言、停止隔离和容量提示通过；100% 同链路也通过。此前把夹具改成 DPI-aware 只能绕过问题，当时夹具已恢复 DPI-unaware，并把生产捕获统一到目标客户区实际自绘范围。Win7 实机当时未验收，现在已无该端可验收 | 历史证据 `artifacts/e2e/20260915-223356/winforms-person-detection.json`；`artifacts/e2e/20260915-222848/winforms-person-detection.json`；入口 `visionguard-e2e -Mode WinFormsPersonDetection` 与 `detector/windows-winforms-smoke/` 均**已随 WinForms 退役删除，当前无等价入口** |
| WinForms 原生主界面布局（已退役的历史记录） | 历史记录：曾自动验证（真实 Release 窗口）/待 owner 目检 | WinForms 检测端已随 V10 退役，以下为当时的证据：保持 .NET Framework 4.7.2 原生 WinForms 控件与 Win7 兼容；按 WPF 的左监控矩阵、右检查器结构重排为“当前来源 / 运行环境 / 连接”三页。当前来源中的来源管理、采集目标、识别设置和监控控制均采用分区标题与等宽双列；运行环境、连接页也使用同一字段/按钮高度。真实 Release 窗口在当前显示缩放下启动、三个页面切换和正常关闭均通过。该端的 UI 目检需求随退役消失 | `artifacts/e2e/20260916-080406/winforms-release-current-source-200pct.png`；`artifacts/e2e/20260916-080406/winforms-release-runtime-environment-actual-key-200pct.png`；`artifacts/e2e/20260916-080406/winforms-release-connection-200pct.png`；历史命令 `visionguard-build -Target WinForms` **已无对应工程** |
| 设备级命令语义（全部来源，WinForms 时期的历史记录） | 历史记录：曾自动验证（隔离 Server + 真实 WinForms） | 不带 `targetSourceId` 的 `pause` 由 Server 中继到检测端后被回执为“已向全部来源下发停止，结果见各来源状态”，回执不带来源标识；同一轮带 `targetSourceId` 的 `resume` 回执精确回写该来源（并以“未选择捕获区域”如实失败）。这取代了此前“设备级命令被静默落到第一路”的行为。该轮用 WinForms 端取证，退役后 Windows 侧没有等价入口，协议语义本身未变 | 历史证据 `artifacts/e2e/20260915-device-level-command.json`；驱动脚本 `scripts/test-device-level-command.js` **已随 WinForms 退役删除，当前无等价入口** |
| V10 Windows 检测端统一 · WinForms 退役 | 已自动验证（两档构建 + 原生库路径 + 档位 UI + 隔离 Server 链路）/待人工、Win7 真机 | WinForms 检测端退役后 Windows 只剩 `detector/windows-wpf/` 的单一 net472 构建，两档位分别锁定配对：**legacy**（Win7 SP1 x64）= 原生 ONNX Runtime **1.1.0** + 纯 CPU + YOLOv5，**modern**（Win10/11）= 原生 **1.19.0** + DirectML + YOLO26。**已验证**：两档位各自 Release 编译 **0 warning / 0 error**；真实进程模块枚举确认 legacy 档加载 `bin\x64\legacy\native\legacy\onnxruntime.dll`、modern 档加载 `bin\x64\modern\native\modern\onnxruntime.dll`，两档应用根目录均不残留原生库；UI Automation 读到 legacy 档 `YOLOv5su 320` 与 `CPU · Windows 7 固定后端`、modern 档 `YOLO26s 320` 与 `DirectML · GPU 加速`；legacy 档连接页为 `● 已连接`；隔离 Server 下「启动检测端 → 驻留自动启动 → 杀掉检测端 → 驻留存活 → `open-detector` 重新拉起」整条链路通过；生产环境端到端链路曾收到告警与截图静默更新。**待人工/真机**：Windows 7 真机验收（安装、启动、WSS/TLS 证书链、推理、多路帧率与退出清理）由 owner 自行完成，本次只到离机与宿主机取证，不得写成已通过。**已删除的覆盖（净减损，不得写成仍然存在）**：`tests/WindowsConfig.Tests` 的采集几何、共享 settings 合并/迁移与窗口重绑断言，`tests/WinFormsMultiSource.Tests`，以及只依赖静态图片 fixture 的人员检测覆盖，都随本次退役消失，当前无等价入口 | `detector/windows-wpf/bin/x64/{legacy,modern}/VisionGuard.exe` 与各自 `native/{legacy,modern}/onnxruntime.dll`；`artifacts/e2e/v10-wpf-smoke.json`（含 `person` 类命中与逐路 FPS，驱动脚本 `scripts/test-wpf-person-detection.ps1` 已删除）|
| Windows 宿主机约束与显示缩放策略 | 历史记录（原驱动工程已删除） | `WindowsConfig.Tests`（曾随 V10 批次①迁到 net472）覆盖采集尺寸边界、100%/125%/150%/175%/200% 的 DIP→采集像素换算、黑屏判定、窗口重绑歧义、逐路故障与容量提示、报警发件箱及跨端设置合并；`WinFormsMultiSource.Tests` 覆盖协商上限只拒绝新增注册、逐路启停/配置/模型隔离与容量提示。**两个工程均已随 WinForms 退役删除，该模式当前跑不了、也无等价入口，上述断言不再有回归基线**；WPF 的自定义启动 DPI 基线与“变化后停止并重启”策略已删除，界面交由 `app.manifest` 声明的 PerMonitorV2，采集几何独立按实际重绘范围处理。该模式原本只跑宿主机约束，不涉及设备、真机采集或报警链 | `artifacts/e2e/20260915-225619/windows-config-tests.txt`；`artifacts/e2e/20260915-225619/winforms-multi-source-tests.txt`；入口 `visionguard-e2e -Mode WindowsTests` 与两个测试工程均已删除 |
| WinForms 来源协商与上限语义（已退役的历史记录） | 历史记录：曾自动验证（隔离 Server） | 隔离 Server 以 `MAX_SOURCES_PER_DETECTOR=6` 启动时，生产 WinForms 端经真实心跳上报 6 个稳定来源 ID、逐来源检测频率与 `source-control` 能力，接收端角色按同一顺序读回；上限为 4 时同样的 6 路心跳不再发布来源，检查脚本按超时失败，证明 Server 对超限数组整组拒绝而不是静默截断。该结论属 Server 侧语义，仍在 Server 单测覆盖范围内；检测端侧当前无等价入口 | 历史证据 `artifacts/e2e/20260914-winforms-server-push-6sources.json`；`artifacts/e2e/20260914-winforms-server-push-4sources.json`；`artifacts/e2e/20260914-winforms-server-push-overlimit.log`；驱动脚本 `scripts/test-winforms-server-push.js` **已随 WinForms 退役删除，当前无等价入口** |
| Android 检测端/接收端启动 | 已自动验证（小米 15） | 指定 `7d3584e1` 设备上的 Debug 构建、安装、清数据、运行时权限、Activity、前台服务和观测窗口无崩溃；不证明完整告警链 | `artifacts/e2e/20260910-105355/summary.json` |
| Android 接收端模拟器启动 | 已自动验证（本次运行） | `VisionGuard_API36` 上 Debug 同签名覆盖安装、运行权限、Activity/进程状态和观测窗口无崩溃；Release APK 已单独构建，因模拟器已有不同签名包而未覆盖安装，未清除应用数据 | `artifacts/e2e/20260911-065633/summary.json`、`artifacts/e2e/20260911-065620/summary.json` |
| Android 两端服务器认证与 MI 6X 运行 | 历史记录（检测端部分已失效） | 2026-09-11 在 MI 6X 上检测端重新编译、安装、启动并记录 `WS 认证成功`，界面显示已连接；接收端改为严格接收当前协议后复测认证成功、丢弃 3 条旧记录。**2026-09-15 复核：该检测端证据早于 `channel` 通道闸门（`ddb06fc`），当前 Server 强制校验 `channel`，而 Android 检测端不发送该字段，因此本端在当前协议下无法认证；按路线图决策 19，Android 检测端已暂缓，此项不再代表当前实现状态。** 接收端本身带 `channel`，认证路径不受影响 | `artifacts/e2e/20260911-001601/summary.json`、`artifacts/e2e/20260911-003309/summary.json`、`artifacts/e2e/20260911-004712/summary.json`、对应 `android-*-logcat.txt` |
| Android NNAPI 实际推理 | 已自动验证（局部） | 归一化 `yolo26n_320` 在小米 15 上完成 CameraX → 预处理 → 真实推理，profile 记录 `NnapiExecutionProvider`；MI 6X 上同一 provider 实际落到 `nnapi-reference`，约 `471–499 ms/次`，慢于原始模型 CPU 回退的 `157–179 ms/次`。provider 命中不再直接写成硬件执行确认，尚未证明 GPU/NPU/DSP、全图下沉、长期稳定性或精度 | `artifacts/e2e/20260910-111229/android-nnapi-evidence-summary.json`、`artifacts/e2e/20260910-231809-mi6x-nnapi/summary.json`、`artifacts/v4/wpf-person-slicefix.json` |
| Android 设备环境发现 | 已自动验证（历史快照） | 小米 15 `7d3584e1` 曾以 ADB `device` 连接，SoC 为 `SM8750`、Android API 36；MI 6X `1fdbd0b1` 用于本轮 NNAPI、检测端和接收端验证。该记录不表示两台设备现在仍连接 | `artifacts/e2e/20260910-102912/inventory.json`、`artifacts/e2e/20260910-231809-mi6x-nnapi/summary.json`、`artifacts/e2e/20260911-004712/summary.json` |

## 当前实现与未来能力边界

- 当前 Server 维护 WS 认证、心跳、告警广播、截图/更新路由和连接在线状态；`online=false` 不等于 `DeviceOfflineAlert` 已生成或送达。
- `DeviceOfflineAlert`、权威多租户事件存储、逐接收端 inbox/ACK、独立 Web Management Console、Linux Edge Detector、多传感器融合和 Qualcomm QNN/NCNN 加速仍属于路线图范围；Windows 检测端到 Server 的持久 outbox/幂等 ACK 已实现，但不等于接收端离线可靠投递完成。
- Windows 静态图片窗口 smoke 证明真实 `WindowHandle` 捕获和人员推理，但不证明动态业务视频、报警截图、中继链路、长时稳定性或 UI 外观；只依赖静态图片 fixture 的那套入口已随 WinForms 退役删除，当前等价覆盖缺位。
- Windows 宿主机约束测试（`WindowsConfig.Tests`、`WinFormsMultiSource.Tests`）已删除：采集几何、共享 settings 合并/迁移、窗口重绑和多来源协调器隔离当前没有自动化回归基线，不能写成仍然受测。
- Android 启动 smoke 不证明摄像头推理、模型下载、Server 连接或接收端展示的完整链路。
- Release 构建不等于正式发行包已通过；正式发行还需要签名、ZIP 清洁度、元数据、上传/部署和公网验证。

## 待人工、真机或生产验证

1. Windows 检测端现代化 UI 的视觉层级、品牌配色和高 DPI 表现仍待 owner 目检。动态窗口移动缩放、遮挡、关闭、最小化故障隔离及恢复已自动验证。WinForms 已退役，其三个标签页不再需要目检。
2. Android 检测端与接收端的真机 UI、归一化模型正式下载、摄像头长时运行、网络切换和温升/资源表现：仍待真机验收。
3. WPF 生产传输服务 → Server → Android 接收端的报警、截图归属和 ACK 已在隔离通道自动贯通；仍需补推理自然触发到传输的单进程测试、真实断网恢复、接收端离线补发和真机通知验收。
4. Win7 SP1 x64 真机验收：驻留程序与 legacy 档检测端的安装、启动、TLS/WSS 证书链、推理、多路帧率、进程握手、网络/休眠恢复和退出清理，由 owner 在真机上完成；驻留的框架/API 迁移已实现，但目标系统验收仍是路线图硬门槛，本报告不得把离机取证写成 Win7 已通过。
5. 生产 VPS、正式发行 ZIP/APK、发布回滚和公网更新接口：本次治理未执行。
6. Windows 端入口缺口：`tests/WindowsConfig.Tests`、`tests/WinFormsMultiSource.Tests` 与静态图片 fixture 人员检测删除后，采集几何/设置迁移/窗口重绑/多来源隔离与 `-Mode WindowsTests`、`-Mode WinFormsPersonDetection` 都没有等价入口，需另行补回或明确接受该覆盖缺失。
7. 下次发行前的动作项：`server/src/index.ts` 的 `app.use('/models', express.static(.../data/models))` 是模型下载的唯一通路，`scripts/publish-release.ps1` 的 `Copy-Models` 只从 `detector\windows-wpf\Assets\` 收集 `*.onnx`。旧的 YOLOv5 模型原先放在已删除的 `detector\windows-winforms\Assets\`，因此**下次发行前必须先把 YOLOv5 的 6 个模型放进 `detector\windows-wpf\Assets\`**（可用 `scripts/export-yolov5-models.py` 导出），否则 legacy 档（Win7）在真机上会因为下载不到模型而无法推理。本报告不声称该步骤已完成。

历史验证记录必须保留原始证据路径，并在重新运行后更新状态；不要仅因日期较新就把历史局部证据升级为完整验收。
