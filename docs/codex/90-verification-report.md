# Verification Report

本文是 VisionGuard 的验证证据台账，不是产品路线图，也不把源码存在、编译成功、帧循环、FPS、界面启动或截图展示写成完整功能通过。每条结论都注明验证范围；详细命令由[运维文档](60-operations.md)和两个保留 Skill 维护。

历史记录只保留结论与证据路径，过程细节不再重述；被替代的入口必须在记录里标明失效，避免被当成仍然可跑。

## 判定词

- **已自动验证**：有可复现命令和机器可读结果，断言覆盖了所写语义。
- **主体实现**：源码已具备功能，但验证范围不足以宣布完整交付。
- **待人工目检**：需要 owner 检查真实 UI、窗口采集或发行包表现。
- **待真机**：需要授权的真实 Android 设备或目标硬件。
- **未实现/未来规划**：当前源码没有该能力，不能用局部 smoke 代替。

## 当前仓库证据

| 范围 | 状态 | 本次证据能证明什么 | 证据 |
|---|---|---|---|
| 文档与治理审核 | 已自动验证（本次运行） | Markdown 链接/锚点、编码、版本、组件、入口、Skill 和产品边界符合契约 | `node scripts/check-docs.js`；`node --test scripts/check-docs.test.js scripts/release-workflow.test.js scripts/version-sync.test.js` |
| PowerShell 脚本解析契约（构建/验证/发布入口） | 已自动验证（本次运行） | `e2e-smoke.ps1`、`publish-release.ps1` 与 `build-all.ps1` 此前**无 BOM 且含中文**，Windows PowerShell 5.1 按 ANSI(936) 解码它们：多字节序列里的 `0x5C` 被当成续行/转义，整份脚本解析失败（实测发布脚本 20 处、e2e 脚本 8 处语法错误，且报错信息与真实原因无关）。三个脚本已改为 UTF-8 with BOM（PS 5.1 读取非 ASCII 的唯一稳定方式），全仓 6 个 `.ps1` 现均 0 解析错误，并由测试固化该约束 | `node --test scripts/check-powershell-encoding.test.js`；`[System.Management.Automation.Language.Parser]::ParseFile` 全仓扫描（修复前后各一次） |
| Server 单测与协议测试 | 已自动验证（本次运行） | 19 项测试通过；覆盖通道不匹配拒绝、报警落盘后 ACK、重复重试不广播、同 ID 冲突拒绝、告警/截图安全、控制请求关联、`device-list` 的 `maxSources`/`sourceLimitExceeded`、超限心跳整组拒绝后保留旧快照并由下一个正常心跳清除标记、驻留-only 设备的业务命令回“该设备当前没有检测端在线”、`MAX_SOURCES_PER_DETECTOR` 默认值与检测端上限（16）一致，以及按推理档位分发更新包的解析（平台别名、档位回落、Android 不受档位影响） | `npm --prefix server test`；`server/test/config.test.ts`、`server/test/update-route.test.ts` |
| 新协议隔离测试通道 | 已自动验证 | 独立 Server 进程使用临时端口和独立数据目录启动，`/health` 返回指定通道；协议测试证明其他通道认证被拒绝 | `scripts/start-isolated-test-server.ps1`；`server/test/control-request.test.ts` |
| V10 Windows 检测端统一 · 批次①（net472 迁移） | 已自动验证（构建 + 真实窗口启动） | `detector/windows-wpf` 目标框架由 `net9.0-windows` 改为 `net472`，新增显式入口 `Program.cs`（设置 TLS 1.2）与 `Runtime/IsExternalInit.cs`、`Runtime/Net472Compat.cs`；修复 36 处 API 差异。5 个依赖工程（`WpfAlertChain.Probe`、`WpfInference.Benchmark`、`windows-wpf-smoke`、`SingleInstance.Probe`、`WindowsConfig.Tests`）同步迁到 net472 并各自构建通过（其中 WinForms 侧工程与 `WindowsConfig.Tests` 已随后退役删除，现存探针见[运维文档](60-operations.md)）。Release 构建 0 错误；真实 Release 窗口启动后正常关闭 | `dotnet build detector/windows-wpf/VisionGuard.csproj -c Release`；`detector/windows-wpf/bin/x64/VisionGuard.exe`；各依赖工程 `dotnet build` |
| V10 · 批次②（双推理档位） | 已自动验证（两档构建 + 原生库路径 + 档位 UI） | 同一份源码按 `OrtProfile` 构建两档，各自锁定同代托管/原生配对：`modern`=托管 ORT 1.19.0 + 原生 1.19.0 + DirectML + YOLO26；`legacy`=托管 ORT 1.2.0 + 原生 1.1.0 + 固定 CPU + YOLOv5。原生库不留在应用根目录，布置到 `native\<档位>\` 后由 `Runtime/NativeLibrarySelector.Initialize()` 用**绝对路径预加载**选择（`PATH` 会被系统目录抢占，已实测）。两档 Release 构建各 0 警告 0 错误；真实进程模块枚举确认 legacy 加载 `bin\x64\legacy\native\legacy\onnxruntime.dll`、modern 加载 `bin\x64\modern\native\modern\onnxruntime.dll`，两档根目录均无 ORT/DirectML 原生库；UI Automation 读到 legacy 档 `YOLOv5su 320` + `CPU · Windows 7 固定后端`、modern 档 `YOLO26s 320` + `DirectML · GPU 加速`。**未验证**：legacy 档在同一台机器上的多路帧率与长时间稳定性（单路推理与报警链已由 owner 在 Win7 实机验收，见下条） | `visionguard-build -Target WPF`；`detector/windows-wpf/bin/x64/{modern,legacy}/`；进程模块枚举与 UI Automation 输出 |
| V10 · 批次③（自研 WebSocket 客户端） | 已自动验证（真实服务器认证 + 长连接） | 新增 `Net/MinimalWebSocketClient.cs`，继承 `System.Net.WebSockets.WebSocket`，对上层与 `ClientWebSocket` 等价；显式 `SslProtocols.Tls12`、HTTP 升级与 `Sec-WebSocket-Accept` 校验、掩码帧读写、ping→pong、close 处理；产物中已无 `websocket-sharp`。modern 档连接生产 Server 持续 70 秒无断开；legacy 档 UI Automation 读到连接页 `● 已连接`、`当前版本 4.4.4`。**未验证**：目标系统上的实际连接、断网恢复与重连退避 | `dotnet build detector/windows-wpf/VisionGuard.csproj -c Release`；真实 Release 窗口 70 秒运行；UI Automation 连接页文本 |
| V10 · 批次④（驻留单次启动与统一命名） | 已自动验证（隔离 Server 全链路 + 真实进程行为） | **角色反转**：检测端启动时探测 `Local\VisionGuard.Resident.SingleInstance`，未运行则用 `UseShellExecute = true` 拉起驻留（写 `%LOCALAPPDATA%\VisionGuard\resident-config.json`），使驻留脱离父进程生命周期并自动登记登录自启；驻留的 WebSocket 与检测端共用同一份 `MinimalWebSocketClient`，同样移除 `websocket-sharp`。**统一命名**：命令 `open-detector`/`close-detector`、组件 `detectorApp`、配置键 `DetectorPath`、应用标识 `Detector`，已贯通驻留、`ConnectionManager`、接收端模型与 UI（单一「打开/关闭检测端」入口）、`check-docs.js` 断言与文档。隔离 Server 上「启动检测端 → 驻留自动启动 → 杀掉检测端 → 驻留存活 → `open-detector` 重新拉起」整条链路通过，回执 `phase=forwarded success=true`；接收端 `testDebugUnitTest` 与 `assembleDebug` 通过 | `visionguard-build -Target Windows`；隔离 Server + 真实 Release 检测端与驻留进程；`%LOCALAPPDATA%\VisionGuard\resident.log`；`HKCU\Software\Microsoft\Windows\CurrentVersion\Run` |
| V10 · WPF 多路人员推理烟测复验（2/3/4 路真实窗口） | 已自动验证（全项通过） | `visionguard-e2e -Mode WpfPersonDetection` 在自身进程内用 `System.Windows.Forms` 打开真实可见顶层窗口（每个显示一张含人图片），逐路 `WindowHandle` 采集后跑 modern 档真实推理。**2 路**：`passed=true`、逐路 2.91 FPS；**3 路**：`default` 58 帧全命中、`signal-2` 84 帧中 44 帧命中、`signal-3` 69 帧全命中，逐路 2.90–2.91 FPS；**4 路（回归基线）**：逐路 2.91 FPS。三轮均 `unexpectedRuntimeErrors` 为空、`ActiveBackend=DirectML`、11 项断言（含 `directMlRuntimeFailureIsolated`）全为真。**同轮修掉两个烟测自身缺陷**（都不是检测端缺陷，检测端的故障注入与逐路隔离逻辑未改动）：夹具首个窗体在宿主最小化启动时可能继承最小化状态、客户区 0×0，被 `WindowEnumerator` 过滤后导致进程崩溃，已加显式 `WindowState=Normal` 与客户区校验；故障注入用例原先固定用 `windows[0]`/`windows[1]`，而 `closeIsolation` 会先关掉 `windows[1]`，使 2 路时该项必然误判为 false，现改用仍然打开的窗口并在 2 路时给两个来源显式不同身份。**未覆盖**：6 路（默认夹具目录只有 4 张人像图，脚本按设计报错而非降低来源数）、动态视频、其他 GPU、显卡驱动重置、持续运行、报警链与 UI 目检 | `visionguard-e2e -Mode WpfPersonDetection -WpfSourceCount {2,3,4}`；逐轮证据 `artifacts/e2e/20260917-040252/wpf-person-detection.txt`、`artifacts/e2e/20260917-040339/wpf-person-detection.txt`、`artifacts/e2e/20260917-040428/wpf-person-detection.txt`；汇总 `artifacts/e2e/20260917-v10-wpf-smoke-verify.txt` |
| V10 · 档位/模型输出契约（Win7 漏检根因） | 已自动验证（两档真实模型 + 真实图片） | **根因**：`YoloOutputParser` 只按 YOLO26 的 `[1,300,6]` 下标解析，而 legacy 档（Win7）加载的 `yolov5*` 模型输出是通道优先的 `[1,84,N]`（320→2100、640→8400 锚点）。按 `[1,300,6]` 读时「置信度」取到的是框坐标、「class_id」取到的是另一个坐标值，`cls >= 80` 把全部候选丢掉：不报错、不出框、不推送。现场表现与 owner 在 Win7 上看到的“能取窗口、能起推理、画面里有目标却检测不到也不推送”完全一致。**修复**：解析器改为按输出长度分档（`Detect` 只接受 1800 与 84×{2100,8400}，其余显式抛错，不再退化），YOLOv5 分支按类做 NMS（模型图内无 NMS）后再取前 5；`MonitorService` 在输出形态变化时记录一行档位/形态诊断；`NativeLibrarySelector` 在建会话后校验实际加载的原生库确实来自本档位目录（机器上存在系统级同名 `onnxruntime.dll` 时实测会按模块名抢占，导致一推理就无诊断信息的进程终止）。**证据**：新增 `-Mode WpfParserContract` 对两档各自构建探针并断言：形态识别、形态与本档位一致、未知长度被拒、原生库来自档位目录、真实图片解析出 `person` 且置信度 ≥0.5、框在画面内。legacy `yolov5nu_320`→`[1,84,2100]`、`person` 0.867/0.672；legacy `yolov5nu_640`→`[1,84,8400]`、`person` 0.633；modern `yolo26n_320`→`[1,300,6]`、`person` 0.944/0.842；modern `yolo26n_640` 同通过。**修复前反证**：同一图片用旧解析逻辑在 legacy 模型上得到 228 个候选全部落进「class_id 越界」分支、0 个有效检测（Python 侧复刻复算）。**未覆盖**：Win7 目标系统本身、动态视频、报警推送、UI 显示与多路帧率 | `visionguard-e2e -Mode WpfParserContract`；`artifacts/e2e/20260917-060825/summary.json`、`parser-contract-{legacy,modern}.json`（同轮另有 `20260917-060703`）；`tests/WpfInference.Benchmark`（`--parser-contract`） |
| Win7 SP1 x64 legacy 档检测链 | 待人工/真机 → **owner 实机验收通过**（2026-09-17，owner 报告） | 解析层按 `[1,84,N]` 分档修复并重出 legacy 产物后，owner 在 Windows 7 SP1 x64 上确认：画面目标可被检测，报警经 Server 送达接收端，链路验收通过。**同一轮 owner 另确认**：Win7 端 UI 目检正常；多路来源可同时检测；连续运行稳定性正常。**证据归属**：全部由 owner 在本机实机操作确认，本报告不声称由自动化采集，也没有仓库内产物路径；来源数量、运行时长与逐路帧率等量化数字待 owner 补充后再写入。**明确未覆盖**：断网恢复、休眠/唤醒恢复、驻留进程握手与 `open-detector`/`close-detector` 远程开关、退出清理（仍列在待验证项）。修复内容与自动化证据见上方「V10 · 档位/模型输出契约」行 | owner 实机操作（无仓库内产物路径） |
| 驻留程序被检测端拉起（含 Win7 失败根因） | 已自动验证（两档真实产物 + 隔离 Server + 服务端视角）；**owner 已在 Win7 实机验收通过**（2026-09-17） | **owner 在 Win7 报告驻留没起来且接收端看不到入口，Win10 正常。查实两个独立缺陷**：① 检测端每次启动重写驻留配置时用了 XML 转义，Windows 路径里的反斜杠被写成 JSON 非法转义（`C:\Users` → 未转义 `\U`），驻留在 `JavaScriptSerializer.Deserialize` 处 `fatal` 退出；Win10 之所以“正常”只是因为那份配置还是旧的合法文件，一旦检测端重写就会同样失败。② 档位产物里根本没有驻留：只有发布 ZIP 才合并驻留目录，本地/离线整体拷贝 `bin\x64\<档位>\` 必然缺 `VisionGuard.Resident.exe`，而拉起失败只写 `Debug.WriteLine`，Release 包在用户机器上完全静默。**修复**：配置改用 JSON 序列化器生成并回读自检；驻留随构建进入每个档位输出目录，发布预检要求包根同时含驻留 EXE 与配置；`ResidentLauncher` 暴露状态快照并写 `resident-launch.log`，连接页新增“驻留程序”状态行（只表达状态，不含说明文案）与刷新按钮；探针 `VisionGuard.exe --resident-launch <report.json> [wait-ms]` 把拉起结论写成可机读 JSON。**证据**：`visionguard-e2e -Mode ResidentLaunch` 两档各自 PASS（`components.resident = running` 由服务端 device-list 断言）；修复前同一探针在 Win10 上也复现了 fatal（`resident.log` 记录 `无法识别的转义序列`）；owner 随后在 Win7 实机确认驻留拉起与接收端入口可用（owner 报告，无仓库内产物路径）。**未覆盖**：远程 `open-detector`/`close-detector` 的实际动作、登录自启、重启后恢复 | `visionguard-e2e -Mode ResidentLaunch`；`artifacts/e2e/20260917-065857/`（同轮另有 `20260917-063829`、`063959`）、`resident-launch-{modern,legacy}.json`、`resident-visibility-{modern,legacy}.json`、`isolated-server.log`；`scripts/assert-resident-visible.js`；owner 实机确认 |
| 检测端控制台两页合并 + 模型下载/装配重构 | 已自动验证（真实 Release 窗口 + UI Automation 文本取证 + 截图）/待 owner 目检视觉 | **页面合并**：右侧控制台由「当前来源 / 运行环境 / 连接」三页改为「当前来源 / 全局设定」两页，原两页的 XAML 与代码后置文件已删除，`GlobalSettingsViewModel` 只做组合（环境设置仍归 `SettingsViewModel`，连接/驻留/更新仍归 `ServerViewModel`）。**模型清单**：模型资源改成逐行清单，行内给出展示名、`✓ 已下载 / 未下载 / 下载中 n%`、进度条与行内下载按钮（已下载转成不可点的“已就绪”）。**下拉过滤**：来源页模型下拉只列本机已下载模型，选中项不在本机时收敛到第一个已下载模型；一个都没有时下拉禁用并显示下载指引。**证据**：真实 Release 程序 + UI Automation 读到页签只有「当前来源」「全局设定」两个，全局设定页读到 6 个模型行与各自状态；临时移开模型缓存后（跑完立即还原，已核对 3 个模型文件回到原位）来源页模型下拉为空且禁用，提示文案“本机还没有模型：请到「全局设定 → 模型资源」下载后再选”可见。截图 `artifacts/e2e/ui-probe/global-settings.png`、`artifacts/e2e/ui-probe-empty/current-source-no-models.png`。**未覆盖**：视觉比例与配色（owner 目检）、真实下载一个模型的完整过程（网络与进度条动效未走通） | `.local\ui-global-settings-smoke.ps1`、`.local\ui-empty-model-state.ps1`（`.local/` 为本地脚本，未入库）；`artifacts/e2e/ui-probe/`、`artifacts/e2e/ui-probe-empty/` |
| 模型下载与装配（真实生产服务器） | 已自动验证（两档真实产物 + 生产 `/models`） | **owner 报告“下载不了”。复核发现三处真实缺陷并修复**：① 失败原因只写 `Debug.WriteLine`，用户在别的机器上只看到“下载失败”，无法区分网络、证书还是服务端缺文件——现在 `ModelManager.LastFailureReason` 记录具体原因（HTTP 状态码 / 异常类型与消息）并由模型行直接显示；② 服务端断流时按 `totalRead` 部分写入也算成功（`EnsureSuccessStatusCode` 只覆盖响应头），会留下一个损坏模型——现在字节数与 `Content-Length` 不一致即判失败、删掉临时文件并重下；③ **legacy 档列出的是 modern 模型清单**：`ModelManager.ModelKeys` 的静态初始化早于 `NativeLibrarySelector.Initialize()`，读到默认 modern 档，Win7 上会去下载本档位跑不了的 `yolo26*`。档位判定已前移到类型初始化，并新增断言把它固化。**生产服务器实测**：12 个模型键全部 `200`；modern `yolo26n_320` 9815817 字节在 2.5s 内下完、100 次进度回调、文件大小与 `Content-Length` 逐字节一致；legacy 档 `yolov5nu_640`（10849255 字节）与 `yolov5su_320`（35.0 MB）同样通过；临时文件按预期清理；下载完成后本机可用模型清单立即更新。**未覆盖**：Win7 实机上的下载（该机 TLS/证书链仍需 owner 复验，失败时界面现在会直接给出原因）、断点续传（未实现，失败即重下） | `visionguard-e2e -Mode ModelDownload`（两档）；`artifacts/e2e/20260917-073025/model-download-modern.json`、`artifacts/e2e/20260917-073033/model-download-legacy.json` |
| 来源页参数自动保存与采集目标分组 | 已自动验证（真实 ViewModel + 隔离 settings）/待 owner 目检视觉 | 来源页移除“保存此路配置 / 撤销修改”，参数改动即自动保存（防抖 500ms 落盘）；模型、检测类别、阈值、频率、冷却属冷改动，改动后在下一次启动该来源时生效，界面只在参数区下方提示「已保存：阈值、频率、冷却 · 重新启动此来源后生效」；采集目标三件套（窗口 / 选区 / 遮罩）合并为一组，只提供整体“重置”，有遮罩时先确认，重置后立即落盘并重建来源。**同轮修掉一个真缺陷**：协调器的 `StatusChanged`/`FrameProcessed` 处理器用 `Sources.First(...)`，而重建来源是「先 Remove 后 Add」，`Add` 内部会立刻 `RaiseStatus`——于是点“重置”就抛 `NullReferenceException`（探针实测栈：`MultiSourceViewModel.<.ctor>b__18_3`）。现在按“找不到就丢弃”处理，并通过 `Dispatch` 在没有 `Application` 的宿主里同步执行。**证据**：`visionguard-e2e -Mode SourceAutoSave` 12 项断言全通过（无待生效项、改动进待生效列表、防抖前磁盘仍为旧值、防抖后为 61/5/9、待生效文案列出三项、重置清空并立即落盘、待生效列表不含采集目标、`SaveCommand`/`CancelCommand` 已不存在）。**未覆盖**：视觉排版与分组观感（owner 目检）、真实窗口/屏幕选取交互 | `visionguard-e2e -Mode SourceAutoSave`；`artifacts/e2e/source-autosave.json`（另有 `.local` 下的真机 UI 探针记录，未入库） |
| V10 · WinForms 退役 | 已自动验证（两档构建 + 悬空引用清扫）/待人工、真机 | Windows 只剩 `detector/windows-wpf/` 的单一 net472 构建，两档位配对见批次②。退役后全仓悬空引用归零（scripts/.agents/.github 仅剩禁止回退的否定断言），两档构建仍各 0 警告 0 错误。**已删除的覆盖（净减损，不得写成仍然存在）**：`tests/WindowsConfig.Tests` 的采集几何、共享 settings 合并/迁移与窗口重绑断言，`tests/WinFormsMultiSource.Tests`，以及只依赖静态图片 fixture 的人员检测覆盖，当前均无等价入口 | `detector/windows-wpf/bin/x64/{legacy,modern}/` 与各自 `native/{legacy,modern}/onnxruntime.dll`；`visionguard-build -Target Windows` |
| WPF → 隔离 Server → Android 接收端报警链 | 已自动验证（确定性传输链） | `WpfAlertChain.Probe` 复用生产 WPF `ServerPushService` 发送 `person` 报警与真实 PNG；Server 完成落盘、ACK、广播和截图备份，Android API 36 模拟器认证隔离通道、收到报警并缓存同一 `alertId` 截图。该测试与人员推理烟测分别通过，但尚未把推理触发与传输探针合成一个进程内测试 | `artifacts/e2e/20260912-234853/summary.json`；`tests/WpfAlertChain.Probe/`；模拟器日志中的 `6c5dc71a-7c83-4cb5-ae57-0ff82ee89c35` |
| ServerBuild | 已自动验证 | TypeScript 编译成功且 `server/dist/index.js` 存在；不是 HTTP/WS 运行 E2E | `artifacts/e2e/20260910-063530/summary.json`、`server-build.txt` |
| WPF 现代化主界面 | 已自动验证（100% 与 200%）/待 owner 目检 | 100% 下 Release 程序及三个控制台页面的运行、交互与布局通过。200% 复验时曾因自定义 DPI 基线初始化和模态框重入持续弹“调度程序处理已暂停”，删除启动 DPI 基线、停止/重启策略并修复窗口关闭递归后，真实 Release 主程序只有一个顶层窗口、切页有响应、10 秒内正常退出、无 `.NET Runtime`/Application Error/WER 事件。2026-09-16 复核：顶部重复摘要与本机全局启停已移除，来源卡片标题栏各自提供新增/删除图标。**注意：该截图记录的是「右侧控制台两页合并」之前的三页版本（含“运行环境/连接”页）与固定 2 列卡片区，界面结构已再次变更（见下方“来源卡片布局”行），截图不再代表当前界面** | `artifacts/e2e/20260915-225349/wpf-real-window-200pct.png`；`artifacts/e2e/wpf-density-source.png`、`wpf-density-environment.png`、`wpf-density-connection-final.png` |
| 来源卡片布局（自适应网格 + 可拖分隔 + 自动分页） | 已自动验证（布局数学）/待 owner 目检视觉与交互 | **问题**：卡片区写死 2 列、大小与窗口/画面无关，单路时卡片横跨两列（1366px 窗口下约 1030×712），16:9 画面按宽度只用到约 579px 高，卡片内左右与上下留下大片黑边，且来源超过 4 路没有分页、放不下的卡片被窗口裁掉。**修复**：新增 `Views/CardGridPanel.cs`（自定义 `Panel`，按实际可用像素排布，非当前页卡片置为 Collapsed 但采集/推理/报警照常）与 `Services/CardLayoutPlanner.cs`（纯计算求解器，优化目标是「画面实际占用面积 = 单元格面积 × 等比缩放填充率」，单路/单行/单列按画面宽高比收边，末行不满只按实际用到的行分配高度）；`MainWindow` 卡片区与检查区之间加入 `GridSplitter`（宽度写入设置键 `Layout.CardsPanelWidth`，限 620–900，检查区 `MinWidth=320` 由固定 376 变为可压缩），页数 > 1 时显示「第 x/y 页 + 上一页/下一页」页脚。**同轮修掉四个求解器缺陷（均由探针与真实渲染轮次暴露）**：① 评分只看单元格面积时 2 路会选出 512×712 的细高单元格（画面仅填约四成，换个形式重演同一个问题）——改为按「单页容量 × 画面实际占用面积」评分，并先看填充率；② `PageSize` 原按网格容量（`rows*columns`）记录，1 路时会记成 3、页脚与页数算错——改为 `min(网格容量, 来源总数)`；③ 候选枚举用 `pageSize < sourceCount` 提前跳过，导致 8 路在 1 列下的所有行数被跳过、整轮无候选而落到兜底分支（真实渲染实测：6 张卡硬塞一行、每张仅 179px 宽且不显示页脚）——改为枚举「一页装到单页上限」所需的最少行数到高度允许的最大行数；④ 格子与画面比例不匹配时只收高度、不收宽度，2 路在 1920×1080 上留下 1584×891 的格子（画面仅占 34% 高度）——改为哪一边富余就收哪一边（画面大小不变，只是消除黑边）。**证据**：`visionguard-e2e -Mode CardLayoutPlan`（`tests/WpfInference.Benchmark --layout-plan`）38 项断言全通过，覆盖 1/2/3/4/5/8/16 路 × 1366×768/1440×900/1920×1080。代表性结果（16:9 画面，填充率均为 100%）：单路 1030×579、页容量 1；2 路 628×353；4 路 512×288（1366×768）/ 789×444（1920×1080）；8 路与 16 路 493×277、每页 6 路、分别 2 页与 3 页。单路预览高度回归线为 ≥380px（旧固定 2 列布局约 312px）。**同轮真实渲染复核**（隔离 settings、不触碰真实配置，窗口正常打开并按窗口消息正常关闭，退出码 0）：单路配置下卡片边框实测跨 x≈97–954，与求解器预测的 843 DIP 网格吻合、页脚不出现；8 路配置下第一页显示 6 张（3 行 2 列）且页脚出现。**未覆盖**：多显示器与不同 DPI 缩放下的求解结果、翻页时卡片切换与选中来源联动、拖拽手感，以及上表两轮截图的视觉观感——全部需要 owner 目检 | `visionguard-e2e -Mode CardLayoutPlan`；`artifacts/e2e/20260918-033259/card-layout-plan.json`；`.local/layout-smoke/layout-wide-single-source.png`、`layout-8sources-page1-v2.png`；`detector/windows-wpf/Services/CardLayoutPlanner.cs`、`Views/CardGridPanel.cs`、`Views/MainWindow.xaml` |
| 来源卡片多页与 16 路上限（2026-09-20 修复） | 已自动验证（服务端默认值 + 布局数学 + modern Release 构建）；**owner 已目检通过**（2026-09-20） | **问题**：owner 报告「来源卡片不能显示多页，加到 4 个之后没法再添加；暂定上限是 16」。根因不在布局：Server 的 `MAX_SOURCES_PER_DETECTOR` 默认 4，并把 `maxSources=4` 随认证/心跳下发，检测端 `_sourceLimit` 被压到 4、新增按钮 `CanAddSource=false` 且界面上没有任何提示；4 路恰好被求解器排成一页，页脚从不出现。**同轮查实并修掉三处真缺陷**：① `CardGridPanel` 沿可视树上溯取 `ItemsControl.DataContext`，拿到的是窗口级 `MainViewModel` 而不是实现 `ICardPageHost` 的 `MultiSourceViewModel`，宿主永远是 null——按画面比例收边在真实界面上从未生效，只有直接调用求解器的净室探针看着是对的——改为 `CardGridPanel.Host` 依赖属性由 XAML 显式绑定 `MultiSourceVm`；② 页脚可见时 ViewModel 上报 `CardsHost` 高度、面板实际拿到的是 `ItemsControl` 高度，两者相差 44px，同一场景会算出不同的「每页几张」，使页脚页码与实际显示的卡片不符、后面的来源翻不到——尺寸改由 `ItemsControl`（`CardsGrid`）上报，求解器不再自行扣页脚，只保留一份高度基准；③ 单元格比例原本无约束（宽扁容器会把 N 张卡压成一行细条），新增 1:2 ~ 2:1 夹紧（长边不超过短边 2 倍），超出时收窄格子并把多出的空间留成间隔——宁可有间隔也不拉伸卡片；同时修掉「高度装不下理想行数时整列无候选」这个会让窄高窗口掉进「每页一张」兜底的分支。新增按钮不可用时把原因写进提示（“已达服务端上限：服务端允许最多 N 路来源”）。**证据**：`server/src/config.ts` 默认值 4→16，超限语义改由测试显式钉住 `MAX_SOURCES_PER_DETECTOR=4`，`npm --prefix server test` 19 项通过；`visionguard-e2e -Mode CardLayoutPlan` 64 项断言全通过（新增 21 项「卡片比例在 1:2~2:1 内」、宽扁/窄高极端容器 2 项、1:1 画面 3 项；16 路在 1366×768/1440×900/1920×1080 下均为每页 6 路、共 3 页；1:1 画面下 4 路收成 356×356 / 422×422 / 512×512 的方形卡片且单页放得下）；`visionguard-build -Target Windows` 两档 Release 均 PASS（0 警告 0 错误；首次失败是本机 `VisionGuard.exe` 与遗留驻留进程占用 legacy 产物，属文件占用而非编译错误，关闭后重跑即通过）。**线上已部署**：2026-09-20 用 `server/deploy.sh`（`SERVER_INFRA_ENV` 指向 `Wenje-Server/Server-infra/server.local.env`）把新 dist 部署到 `/opt/visionguard-server` 并重启 `visionguard.service`，线上 `dist/config.js` 的默认值已是 16、`/health` 200、接收端自动重连；未做版本发布（`VERSION` 未动、未打 tag、未发 GitHub Release）。**真实渲染复核**（modern 档 Release + `VISIONGUARD_SETTINGS_PATH` 隔离配置预设 16 个来源，真实可见窗口）：页脚出现「第 1/6 页」，UI Automation 实测可见卡片约 523×261.5 DIP（比例 2.0，即被夹紧到上限并留出间隔），点「下一页」后可见卡片由来源 1–3 变为来源 4–6、页脚变「第 2/6 页」——页码与画面内容一致。**未覆盖**：卡片观感与每页张数已由 owner 在 2026-09-20 目检通过（该轮记录的是分页时代的行为，已被同日的「实时预览位与全局来源」改造取代） | `npm --prefix server test`；`artifacts/e2e/20260920-041523/card-layout-plan.json`；`server/deploy.sh`；`server/src/config.ts`、`server/test/config.test.ts`、`detector/windows-wpf/Services/CardLayoutPlanner.cs`、`Views/CardGridPanel.cs`、`Views/MainWindow.xaml`、`Views/MainWindow.xaml.cs`、`ViewModels/MultiSourceViewModel.cs` |
| 实时预览位与全局来源（2026-09-20 改造） | 已自动验证（布局数学 32 项 + 真实窗口交互）；**owner 已目检通过**（2026-09-20） | **需求**：卡片太宽 → 比例压到 1:1.2；卡片内画面区短边要 >320 DIP；取消分页；新增「全局来源」编号卡片视图勾选最多 4 个进入实时预览的来源，其余来源继续推理但不预览。**实现**：`CardLayoutPlanner` 重写为固定网格（1 = 1×1、2 = 1×2 或 2×1、3–4 = 2×2）+ 1:1.2 ~ 1.2:1 比例夹紧；`MainWindow` 的分页页脚换成工具条（「全局来源 / 返回预览」+「实时预览 N/4」+ 超限提示），全局视图是 `GlobalSourcePanel` 自适应铺满的编号卡片网格（不用 `WrapPanel` + `ScrollViewer`：ScrollViewer 给无限高度，面板就铺不满）；`MultiSourceViewModel` 新增 `PreviewSources` 与设置键 `Layout.PreviewSourceIndexes`，未进入预览的来源在 `FrameProcessed` 里跳过位图转换与检测框更新（仍采集/推理/报警），移出预览时释放最后一帧位图；窗口 MinWidth/MinHeight 提到 1200/880，卡片区下限 620→680。**第二轮按 owner 要求压缩卡片 UI**：标题行 36→26、操作行 40→28、内边距 7→3、外边距 3→2、间距 6→4（垂直占位 103→68），底部信息条由两行四段并为一行三段；压缩后最小窗口下 1:1 画面短边为 1 张 717、2 张 420、**4 张 323**（达标 ≥320；1920×1080 下 4 张为 423）。**第三轮按 owner 反馈修正**：① 右侧检查区默认取最窄（320），卡片区改成自适应列占满其余空间；同时修掉宽度绑定——`ColumnDefinition.Width` 是 `GridLength`，早先用 double 绑定 `Layout.CardsPanelWidth` 静默失效（卡片区与检查区各占一半、设置值从未生效），现在改为 `InspectorWidth`（`GridLength`）绑定设置键 `Layout.InspectorPanelWidth`（限 320–900），拖完分隔条把卡片列重置回 `*`、只持久化检查区宽度。② 卡片底部状态信息统一用 `CaptionText` 字号并显式垂直居中，修掉一行三段的错位。③ 全局视图卡片**去掉勾选框**（太小不显眼），选中态改成整块主色实底 + 白字、未选中是卡片表面色 + 次级文字，一眼看出哪 4 个在预览；编号行改成 `#N`，避免与默认名称「来源 N」重复。**证据**：`visionguard-e2e -Mode CardLayoutPlan` 32 项断言全通过（比例区间、排得下不裁剪、短边回归线、宽扁/窄高容器、确定性、退化输入）；`visionguard-build -Target Windows` 两档 Release 0 错误；真实 Release 窗口 + UI Automation：预览视图 4 张卡片、工具条「实时预览 4/4」，切到全局视图显示 16 张编号卡片、`GlobalSourcePanel` 排成 4×4 且铺满卡片区（实测卡片 171×194 DIP、列行间距均为 4），4/4 时点第 5 个被拒绝并显示「实时预览最多 4 个，请先取消一个再勾选」，取消一张后 3/4、再勾回 4/4，返回预览后只排布勾中的来源（勾选持久化生效：重启后仍为上次选择）。**过程中修复**：编辑 `e2e-smoke.ps1` 注释时丢了 UTF-8 BOM（PowerShell 5.1 按 ANSI 解析整份脚本失败），已恢复 BOM 并通过 `check-powershell-encoding.test.js`。**未覆盖**：未选中来源的推理帧率与长期运行、Win7 legacy 档实机（视觉观感与编号卡片点击手感已由 owner 在 2026-09-20 目检通过） | `artifacts/e2e/20260920-051506/card-layout-plan.json`；`.local/layout-smoke/preview-full.png`、`global-view.png`；`detector/windows-wpf/Services/CardLayoutPlanner.cs`、`Views/CardGridPanel.cs`、`Views/GlobalSourcePanel.cs`、`Views/MainWindow.xaml`、`Themes/DarkTheme.xaml`、`ViewModels/MultiSourceViewModel.cs`、`tests/WpfInference.Benchmark/Program.cs` |
| Windows 7 重复启动崩溃修复（2026-09-20） | 已自动验证（本机复现 + 真实进程行为）；**owner 已在 Win7 实机确认启动与 UI 显示正常**（2026-09-20，推理未测） | **问题**：owner 在 Win7 模拟器上打开检测端失败，弹出 .NET Framework 崩溃对话框：「Cannot set Visibility to Visible or call Show, ShowDialog, Close, or WindowInteropHelper.EnsureHandle while a Window is closing.」。**根因**：`App.xaml` 声明了 `StartupUri="Views/MainWindow.xaml"`，而 `App.OnStartup` 的「已有实例在运行」分支先调 `Shutdown()` 再返回——`StartupUri` 的窗口创建发生在 `OnStartup` 返回之后，于是应用已进入关闭状态却仍去显示窗口；该分支还排在异常处理器注册之前，异常绕过处理直接弹出 CLR 对话框。**修复**：主窗口改由 `App.OnStartup` 显式创建（`App.xaml` 去掉 `StartupUri`）；异常处理器移到 `OnStartup` 最前面；非主实例分支不再创建任何窗口、干净退出；关闭过程中不再弹错误框（`MessageBox` 自己也是窗口）；新增落盘崩溃日志 `%LOCALAPPDATA%\VisionGuard\detector-crash.log`——`LogManager` 只写 `Debug.WriteLine`，Release 下崩溃此前完全静默。**过程中的自证**：第一版修复误用 `StartupUri = null`（`Application.StartupUri` 不接受 null，抛 `ArgumentNullException`），正是新加的崩溃日志把这条记录了下来，随后改为显式创建主窗口。**证据**：本机 Release 复现「启动第一个实例 → 再启动第二个」——第二实例 `exitCode=0` 干净退出、第一个实例不受影响、日志写入「启动退出：已有 VisionGuard 实例在运行」；主窗口仍正常显示（标题 VisionGuard、工具条「全局来源」「实时预览 4/4」、4 张卡片）。`visionguard-build -Target Windows` 两档 Release 0 警告 0 错误。**Win7 实机复验**：owner 在 Win7 模拟器上确认检测端启动与 UI 显示正常（2026-09-20），原先的崩溃对话框不再出现。**未覆盖**：Win7 上的推理、报警链与长时间运行（owner 本轮明确跳过）；若日后再次出现启动失败，用崩溃日志取回真实堆栈 | `detector/windows-wpf/App.xaml`、`App.xaml.cs`；`%LOCALAPPDATA%\VisionGuard\detector-crash.log` |
| WPF 采集尺寸与遮罩窗口约束 | 已自动验证（边界与实际拖拽）/待人工目检（遮罩布局） | `CaptureSizeConstraints` 断言宽高分别严格大于 100 物理像素：`100×101`、`101×100`、`99×500` 拒绝，`101×101` 接受；200% DPI 映射覆盖 `50 DIP → 100 px` 拒绝与 `50.5 DIP → 101 px` 接受。Win32 创建的 `100×150` 可见窗口未出现在实际枚举结果中；在 Release 程序中实际拖出 `93×93 px` 选区后选择器保持打开并提示“选区过小” | `visionguard-build -Target WPF`；Windows UI Automation + Win32 鼠标输入；`MaskEditorWindow.xaml` 最小尺寸 |
| Windows 驻留程序 | 已自动验证（构建 + 隔离链路） | 驻留已迁到 .NET Framework 4.7.2 x64，`visionguard-build` 构建 0 警告 0 错误。独立通道验证驻留认证、组件心跳、Server 生命周期命令转发及带 `requestId` 的完成回执，并用不存在的检测端路径断言失败原因 | `visionguard-build -Target WindowsResident`；`scripts/test-windows-resident.js`；`detector/windows-resident/bin/Release/net472/` |
| Win7 SP1 x64 前置环境 | 已自动验证 | 目标系统确认为 Windows 7 SP1 x64；KB4490628、KB4474419、KB3140245、WinHTTP 32/64 位 TLS 1.2（均为 2560）、KB4019990 与 .NET Framework 4.7.2（Release 461814）全部通过机器判定 | `scripts/check-win7-prerequisites.ps1`；`artifacts/e2e/win7-prerequisites.json` |
| Android 接收端控制完成语义 | 已自动验证（单元/构建） | 只把字段完整的 `completed` 回执映射为结构化结果，缺失阶段的旧载荷被拒绝；冷却档位保留设备当前值、缺少 `maxSources`/`sourceLimitExceeded` 的旧记录按默认值解析；Debug 单测与 APK 构建通过 | `receiver/android/gradlew.bat testDebugUnitTest assembleDebug` |
| Android 检测端/接收端启动 | 已自动验证（小米 15） | 指定 `7d3584e1` 设备上的 Debug 构建、安装、清数据、运行时权限、Activity、前台服务和观测窗口无崩溃；不证明完整告警链 | `artifacts/e2e/20260910-105355/summary.json` |
| Android 接收端模拟器启动 | 已自动验证 | `VisionGuard_API36` 上 Debug 同签名覆盖安装、运行权限、Activity/进程状态和观测窗口无崩溃；Release APK 已单独构建，因模拟器已有不同签名包而未覆盖安装 | `artifacts/e2e/20260911-065633/summary.json`、`artifacts/e2e/20260911-065620/summary.json` |
| Android NNAPI 实际推理 | 已自动验证（局部） | 归一化 `yolo26n_320` 在小米 15 上完成 CameraX → 预处理 → 真实推理，profile 记录 `NnapiExecutionProvider`；MI 6X 上同一 provider 实际落到 `nnapi-reference`，约 `471–499 ms/次`，慢于原始模型 CPU 回退的 `157–179 ms/次`。provider 命中不等于硬件执行确认，尚未证明 GPU/NPU/DSP、全图下沉、长期稳定性或精度 | `artifacts/e2e/20260910-111229/android-nnapi-evidence-summary.json`、`artifacts/e2e/20260910-231809-mi6x-nnapi/summary.json`、`artifacts/v4/wpf-person-slicefix.json` |
| Android 设备环境发现 | 已自动验证（历史快照） | 小米 15 `7d3584e1` 曾以 ADB `device` 连接，SoC `SM8750`、Android API 36；MI 6X `1fdbd0b1` 用于 NNAPI 与两端验证。该记录不表示两台设备现在仍连接 | `artifacts/e2e/20260910-102912/inventory.json`、`artifacts/e2e/20260910-231809-mi6x-nnapi/summary.json`、`artifacts/e2e/20260911-004712/summary.json` |

## 历史记录（入口已失效，仅保留结论与证据）

- **WinForms 检测端多来源主体**：200% 缩放下 DPI-unaware net472 夹具的 6 路 YOLOv5 CPU 推理每路命中 `person`、无黑屏，约 2.91–2.94 FPS，子区域裁剪、停止隔离与容量提示通过；100% 同链路也通过。证据 `artifacts/e2e/20260915-223356/winforms-person-detection.json`、`artifacts/e2e/20260915-222848/winforms-person-detection.json`；入口 `-Mode WinFormsPersonDetection` 与 `detector/windows-winforms-smoke/` **已随退役删除**。
- **WinForms 原生主界面布局**：按 WPF 结构重排为“当前来源 / 运行环境 / 连接”三页，真实 Release 窗口启动、切页与关闭通过。证据 `artifacts/e2e/20260916-080406/winforms-release-current-source-200pct.png`、`artifacts/e2e/20260916-080406/winforms-release-runtime-environment-actual-key-200pct.png`、`artifacts/e2e/20260916-080406/winforms-release-connection-200pct.png`；`visionguard-build -Target WinForms` **已无对应工程**。
- **WinForms 设备级命令语义**：不带 `targetSourceId` 的 `pause` 回执“已向全部来源下发停止”，带 `targetSourceId` 的 `resume` 精确回写该来源；取代了此前“设备级命令被静默落到第一路”的行为。证据 `artifacts/e2e/20260915-device-level-command.json`；驱动 `scripts/test-device-level-command.js` **已删除，Windows 侧当前无等价入口**，协议语义本身未变。
- **Windows 宿主机约束与显示缩放策略**：`WindowsConfig.Tests` 曾覆盖采集尺寸边界、100%–200% 的 DIP→采集像素换算、黑屏判定、窗口重绑歧义、逐路故障与容量提示、报警发件箱及跨端设置合并；`WinFormsMultiSource.Tests` 曾覆盖协商上限只拒绝新增注册、逐路启停/配置/模型隔离与容量提示。**两个工程均已删除，上述断言不再有回归基线**，`-Mode WindowsTests` 一并移除。证据 `artifacts/e2e/20260915-225619/windows-config-tests.txt`、`winforms-multi-source-tests.txt`。
- **WinForms 来源协商与上限语义**：隔离 Server 以 `MAX_SOURCES_PER_DETECTOR=6` 启动时，6 路心跳被识别为 6 个稳定来源；上限为 4 时同样心跳整组被拒而不是静默截断。该结论属 Server 侧语义，仍在 Server 单测覆盖内；检测端侧无等价入口。证据 `artifacts/e2e/20260914-winforms-server-push-6sources.json`、`artifacts/e2e/20260914-winforms-server-push-4sources.json`、`artifacts/e2e/20260914-winforms-server-push-overlimit.log`；驱动 `scripts/test-winforms-server-push.js` **已删除**。
- **V10 端到端回归（退役前）**：2 路真实窗口烟测 13/14 项通过，逐路命中与 2.9 FPS 达标，唯一未通过的 `directMlRuntimeFailureIsolated` 已于 2026-09-17 查明是**夹具窗口未真正显示（客户区 0×0）**所致，不是产品缺陷；产品侧故障注入与逐路隔离逻辑未改动。证据 `artifacts/e2e/v10-wpf-smoke.json`；驱动 `scripts/test-wpf-person-detection.ps1` 与夹具 `detector/windows-winforms-smoke/` **均已删除**。
- **WPF 四路/三路人员推理（夹具形态变更前）**：200% 缩放下以 DPI-unaware net472 窗口工具承载 6 个独立窗口，规范化后 DirectML 每路约 3 FPS 且每帧命中 `person`（60/60、87/87、92/92、98/98、103/103、70/70），子区域裁剪、移动缩放、遮挡、最小化恢复、关闭隔离与停止/重配隔离全部通过；三路图片推理一轮断言的是当时生效的“CPU 多路拒绝”边界，该边界已于 2026-09-13 撤销。证据 `artifacts/e2e/wpf-window-person-detection-client-plane-200pct.json`、`artifacts/e2e/wpf-window-person-detection-client-plane-pass.json`、`artifacts/e2e/20260910-063535/summary.json`。
- **WPF 窗口重绑与故障隔离**：窗口恢复覆盖精确身份命中、唯一稳定身份下标题变化重绑、同身份多候选拒绝与旧配置同名窗口拒绝；受控 DirectML 引擎在真实定时链路抛错后故障保留在对应来源、其余来源继续运行。窗口重绑断言原由 `tests/WindowsConfig.Tests`（**已删除**）覆盖；尚未模拟显卡驱动重置或物理设备丢失。证据 `artifacts/e2e/20260914-113036/wpf-person-detection.json`。
- **WPF DirectML/CPU 负样本对照**：当前界面截图样本在两后端均无有效检测；不证明人员召回或代表性精度。证据 `artifacts/v3/wpf-directml-cpu-parity.json`。
- **Android 两端服务器认证与 MI 6X 运行**：2026-09-11 检测端在 MI 6X 上认证成功，接收端严格接收新协议后复测成功。**2026-09-15 复核：该检测端证据早于 `channel` 通道闸门（`ddb06fc`），当前 Server 强制校验 `channel` 而 Android 检测端不发送该字段，本端在当前协议下无法认证；按路线图决策 19 Android 检测端已暂缓。** 接收端带 `channel`，认证路径不受影响。证据 `artifacts/e2e/20260911-001601/summary.json`、`20260911-003309/summary.json`、`20260911-004712/summary.json`。

## 当前实现与未来能力边界

- 当前 Server 维护 WS 认证、心跳、告警广播、截图/更新路由和连接在线状态；`online=false` 不等于 `DeviceOfflineAlert` 已生成或送达。不把源码存在当成功能已交付。
- `DeviceOfflineAlert`、权威多租户事件存储、逐接收端 inbox/ACK、独立 Web Management Console、Linux Edge Detector、多传感器融合和 Qualcomm QNN/NCNN 加速仍属路线图范围；Windows 检测端到 Server 的持久 outbox/幂等 ACK 已实现，但不等于接收端离线可靠投递完成。
- Windows 静态图片窗口 smoke 证明真实 `WindowHandle` 捕获和人员推理，但不证明动态业务视频、报警截图、中继链路、长时稳定性或 UI 外观。
- 采集几何、共享 settings 合并/迁移、窗口重绑和多来源协调器隔离当前没有自动化回归基线，不能写成仍然受测。
- Android 启动 smoke 不证明摄像头推理、模型下载、Server 连接或接收端展示的完整链路。
- Release 构建不等于正式发行包已通过；正式发行还需要签名、ZIP 清洁度、元数据、上传/部署和公网验证。

## 待人工、真机或生产验证

1. Windows 检测端现代化 UI 的视觉层级、品牌配色和高 DPI 表现仍待 owner 目检；动态窗口移动缩放、遮挡、关闭、最小化故障隔离及恢复已自动验证。**来源卡片布局已目检通过**（2026-09-20）：4 张实时预览的卡片比例、画面空间、状态信息对齐、全局来源的铺满与选中配色、检查区默认最窄，以及 4/4 时点第 5 个被拒绝的提示，均由 owner 在真实窗口确认。**仍未覆盖**：未选中来源的推理帧率与长期运行、Win7 legacy 档实机、多显示器与不同 DPI 缩放下的求解结果。
2. Windows 7 SP1 x64 目标系统验收：**单路检测、报警送达接收端、Win7 端 UI 目检、多路来源同时检测、连续运行稳定性、驻留拉起与接收端入口已由 owner 于 2026-09-17 实机验收通过**（见「当前仓库证据」中「Win7 SP1 x64 legacy 档检测链」与「驻留程序被检测端拉起」两行；量化数字待补）。仍待覆盖的子项：远程 `open-detector`/`close-detector` 的实际动作、驻留登录自启与重启后恢复、断网恢复、休眠/唤醒恢复、退出清理。本报告对上述未覆盖子项不声称已通过。
3. Android 检测端与接收端的真机 UI、归一化模型正式下载、摄像头长时运行、网络切换和温升/资源表现。
4. WPF 生产传输服务 → Server → Android 接收端的报警、截图归属和 ACK 已在隔离通道自动贯通；仍需补推理自然触发到传输的单进程测试、真实断网恢复、接收端离线补发和真机通知验收。
5. 生产 VPS、正式发行 ZIP/APK、发布回滚和公网更新接口：尚未执行。
6. 首次连接 Server 的 Windows 检测端部署缺口：生产环境需要部署含 `wpf-legacy` 平台键与按档位分发逻辑的 Server 版本。
7. 发行前模型就位（**已完成**）：`server/src/index.ts` 的 `app.use('/models', express.static(.../data/models))` 是模型下载的唯一通路，`scripts/publish-release.ps1` 的 `Copy-Models` 只从 `detector\windows-wpf\Assets\` 收集 `*.onnx`（该目录受 `.gitignore` 排除、不入版本控制），旧的 YOLOv5 模型原先放在已删除的 `detector\windows-winforms\Assets\`。2026-09-17 已把两个档位的 12 个模型全部取回构建机的 `detector\windows-wpf\Assets\`（modern 档 YOLO26 六个 + legacy 档 YOLOv5 六个，共约 530 MB），并为发布脚本补上预检闸门 `Test-RequiredModels`：Windows/WPF 目标下逐项校验这 12 个模型是否存在且不小于 1 MB，缺任一项即中止发布，不再允许静默少带模型。已用「临时移走一个模型」做负向验证，预检确实以退出码 1 失败并列出缺失文件名。

历史验证记录必须保留原始证据路径，并在重新运行后更新状态；不要仅因日期较新就把历史局部证据升级为完整验收。
