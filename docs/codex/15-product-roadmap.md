# VisionGuard 产品路线图

> 状态：未来规划，不代表当前实现
>
> 更新日期：2026-09-10
>
> 事实边界：当前实现以源码和其他 `docs/codex/` 专题为准；本文是产品方向、阶段顺序与验收闸门的唯一事实来源。

阅读顺序：第 1–7 节说明产品方向和架构约束；[第 8 节](#8-50-以内更新视觉端驻留远控硬件加速与四路检测)维护已确认的 5.0 以内更新；[第 9 节](#9-平台与硬件演进路线)维护平台与硬件演进阶段；第 10 节列出范围限制；[第 11 节](#11-当前执行顺序与后续规划)说明当前执行顺序。

## 1. 目标定位

VisionGuard 将从“多端视觉检测与告警应用”演进为“边缘智能探测平台”。产品核心不是某个 YOLO 模型或某一种摄像头，而是稳定完成以下闭环：

```text
多源感知 -> 边缘推理与融合 -> 标准报警事件 -> 可靠加密传输 -> 用户处置 -> 设备持续运维
```

长期能力分为三层：

- **Detector Platform**：视觉、毫米波、PIR、门磁、振动、声音和环境传感器的统一接入、健康检查、推理与融合。
- **Reliable Event Network**：统一事件信封、本地 outbox 和幂等 ACK；所有公网业务数据统一通过 Server，使用 HTTPS/WSS `443` 加密传输，由 Server 负责权威入库、可靠投递、设备在线感知和离线兜底，不再规划 P2P、ICE、STUN 或 TURN 通道。
- **Device & Fleet Cloud**：Server 与 Web 控制台属于完整交付系统，以控制台为最高权限管理入口，覆盖账号租户、场地、探测器、传感器、事件数据、配置、模型、OTA、调试、审计、运行指标和商业配额。

### 1.1 产品对象

产品不再把 WinForms、WPF 和 Android 检测端仅称为泛化“客户端”，而是统一纳入探测器体系：

| 对象 | 定义 |
|---|---|
| Detector | 能够产生标准 Observation / AlertEvent 的探测器总称 |
| Visual Detector | 现有 WinForms、WPF、Android 检测端；以屏幕、窗口或摄像头视觉推理为核心 |
| Edge Detector | 未来 Linux ARM64 开发板探测器；可组合视觉、毫米波、PIR、门磁和环境传感器 |
| Sensor | 被探测器采集和管理的单一感知来源，本身不一定具备完整联网与事件判断能力 |
| Receiver | 接收、解密、展示和处置报警的终端 |
| Web Management Console | 管理场地、探测器、传感器、配置、模型、OTA 和审计的 Server Web 控制台 |

三类现有 Visual Detector 是长期正式产品对象，后续应逐步接入统一设备身份、能力声明、Event Protocol、Management Agent 和 Web 控制台；不能因为开发板成为未来重点而将现有视觉探测器降级为临时兼容实现。

## 2. 产品原则

1. **可靠送达高于链路优化**：所有公网业务统一经 Server，优先保证报警入库、送达、可追踪和可诊断，不以 P2P 直连率或节省中继带宽增加系统复杂度。
2. **边缘判断优先**：原始传感器数据尽量留在现场；向外发送标准事件和必要证据，减少隐私风险与网络依赖。
3. **协议先于客户端**：所有检测端共享设备身份、能力声明、事件、ACK 和错误语义；界面和运行时可以不同。
4. **视觉是证据，不是全部事实**：使用低功耗触发传感器发现异常，使用视觉、毫米波或其他确认传感器提高可信度。
5. **漏报优先于误报**：允许在可管理范围内误报，不接受已知、可避免或静默发生的漏报；通过高召回目标、冗余传感器、故障报警和真实样本验证降低漏报风险，但不做无法证明的“绝对零漏报”承诺。
6. **Win7 当前实现与计划边界**：当前可验证的 Win7 兼容线是 Windows WinForms / .NET Framework 4.7.2 Visual Detector；Windows 驻留程序当前基于 .NET 9，尚未兼容 Win7，但驻留远控在后续交付前必须补齐 Win7 SP1 x64 兼容；WPF、Android、Server、Web 控制台和未来硬件不承担 Win7 兼容义务。
7. **失败必须可诊断并报警**：每个阶段都要能区分感知失败、融合失败、认证失败、Server 连接失败、设备离线和接收端离线；会造成探测盲区的故障本身也是报警事件。
8. **先证明场景价值**：开发板是验证载体，不是产品定义；首个商业版本必须聚焦一个明确场景和可量化效果。

### 2.1 商业分层

| 产品层 | 组成 | 商业定位 |
|---|---|---|
| 免费版：纯软件视觉方案 | 当前已实现的 WinForms、WPF、Android Visual Detector，以及维持现有纯软件报警闭环所需的 Server 与 Receiver 能力 | 不接入检测硬件探测器时长期免费；云端存储、Server 转发和历史数据仍需合理配额，避免产生无限持续成本 |
| 付费版：硬件探测器方案 | 免费版全部能力 + Edge Detector、集成传感器检测单元或其他检测硬件探测器 | 系统一旦注册并启用检测硬件探测器即进入付费版；提供硬件设备管理、传感器拓扑、融合检测、独立运行、OTA 和诊断能力 |

免费版以目前已经实现的纯软件视觉方案为边界；系统一旦接入检测硬件探测器，即进入付费版。判定依据是系统中是否激活硬件探测器，不取决于硬件由 VisionGuard 自研、合作厂商提供还是兼容第三方设备。免费与付费共享 Detector Protocol、Event Protocol、账号、Server、Receiver 和 Web 控制台底座，不建立两套互不兼容的系统。具体免费配额、硬件售价或授权方式、服务期限和增值服务在试点成本数据形成后确定。

免费版是“免费使用的源码可见软件”，不等于开源软件。当前主线使用 `VGSAL-1.0`：纯软件视觉方案允许个人和组织免费内部使用；接入硬件探测器、再分发、对外托管或集成到第三方产品需要商业授权。`c43c0ff122043d477b442b7507d193b62ea321bb` 及其祖先和 `v4.4.3` 及更早发行版本继续适用发布时的 MIT License。许可证切换不能替代第三方依赖、模型、数据和素材的商业兼容性审查。

### 2.2 交付范围与目标市场

整套系统均属于产品交付物，包括：

- Visual Detector；
- Edge Detector 软件、固件与硬件；
- Android Receiver；
- VisionGuard Server；
- Web Management Console；
- 模型、升级、部署、备份恢复和诊断工具。

首个销售市场暂定中国大陆。产品设计需从第一阶段考虑个人信息告知、数据最小化、保存期限、删除机制、账号注销、访问审计和安全事件处置。是否提供客户私有化部署作为后续商业选项单独评估，不预设为首发必需能力。

## 3. 目标架构

```text
现场传感器
  摄像头 / 毫米波 / PIR / 门磁 / 振动 / 环境量
                         |
                         v
Edge Detector 或现有 Visual Detector
  采集 -> 推理 -> 融合 -> Event -> 本地加密 outbox
                         |
                         | HTTPS/WSS 443 / 设备身份 / ACK
                         v
                 VisionGuard Server
 权威事件存储 / 证据 / 在线状态 / 离线报警 / 去重 / 重试 / 审计
                    |             |
          WSS / 系统推送          | Web API
                    v             v
             Android Receiver   Server Web Console
           去重 -> 报警 -> ACK   场地 / 设备 / 数据 / 配置 / OTA / 调试
```

统一传输路径计划为：

1. 探测器主动建立出站 WSS `443`，用于认证、心跳、报警和控制；
2. 截图、证据、模型和固件使用 HTTPS `443` 上传或下载；
3. WSS 暂不可用时，探测器保留本地 outbox，并可按协议使用 HTTPS 补发事件；
4. Server 权威入库后再向在线 Receiver 分发，并对离线 Receiver 使用适合中国大陆终端的系统推送通道唤醒；
5. 实时画面若进入产品范围，只允许通过受控 Server 转发，并独立核算带宽、权限和容量，不重新引入端到端直连。

不再规划 P2P、ICE、STUN 或 TURN，减少跨端原生依赖、NAT 穿透测试和多路径状态机。代价是 Server 带宽、容量和可用性责任上升，因此必须优先建设持久化 outbox、幂等入库、逐接收端 ACK、容量监控、备份恢复和独立的 Server 外部健康监测。

## 4. 统一探测模型

### 4.1 Observation

传感器适配器只产生观察值，不直接决定云端接口或接收端 UI：

```text
Observation
- sourceId
- sensorType
- occurredAt
- confidence
- measurements
- region
- health
```

### 4.2 AlertEvent

融合层把一个或多个观察值变成标准报警：

```text
AlertEvent
- eventId
- detectorId
- eventType
- severity
- confidence
- evidence[]
- occurredAt
- expiresAt
```

同一个 `eventId` 允许重试，但接收端只能落库和提示一次。多接收端分别维护 ACK 状态，探测端或服务器不能用单个“已发送”布尔值替代逐设备送达结果。

### 4.3 系统状态报警

Server 必须把设备连通性当作受监控信号，而不是只在设备列表上显示灰色状态：

- 探测器按设备类型上报心跳和业务状态，Server 维护 `online -> suspected_offline -> offline -> recovered` 状态机；
- 超过可配置的失联阈值后，由 Server 生成标准 `DeviceOfflineAlert`，进入与检测报警相同的入库、分发、ACK、重试和审计链路；
- 同一次失联只生成一个活动报警，避免重连抖动造成报警风暴；恢复连接后生成关联的 `DeviceRecoveredEvent`，保留失联开始、恢复时间和持续时长；
- 离线报警只能说明设备、供电、现场网络或客户端进程至少一项异常，在设备恢复并上报诊断前不得伪造具体原因；
- 传感器断开、摄像头不可用、推理线程停滞、存储不足和 outbox 堵塞等可能导致漏报的状态，也必须形成可送达的故障报警；
- Receiver 不在线时，Server 保留待送达状态，并使用系统推送或下次连接补发，不能因为接收端离线静默丢弃；
- Server 自身宕机时无法由自身产生报警，必须由独立于 VisionGuard 业务进程的外部监控检查公网健康、队列和数据库，并通过另一条通知通道告警。

失联阈值和抖动宽限需要按探测器类型配置。当前 4.x 的 45 秒幽灵阈值可作为兼容起点，但商业硬件最终值必须根据心跳周期、网络环境和可接受盲区通过故障注入确定。

## 5. 开发板边缘探测器方向

### 5.1 原型定位

第一代 Edge Detector 采用 Linux ARM64 开发板验证产品闭环，至少包含：

- 一个摄像头输入；
- 一个低功耗触发传感器，优先毫米波或 PIR；
- 一个确定性数字输入，优先门磁；
- 本地 ONNX 推理与两类传感器融合；
- 本地持久化 outbox；
- WSS/HTTPS 可靠传输、网络切换和断线补发；
- 独立设备身份和独立联网；
- 接受 Server Web 控制台的远程配置、诊断、模型更新和 OTA；
- 72 小时连续运行、断网恢复和异常断电验证。

首版不同时接入大量传感器，避免驱动、供电、安装和测试矩阵失控。

### 5.2 商业硬件边界

开发板原型通过后，商业硬件再评估工业 SOM 或定制载板。正式设备至少需要 eMMC、硬件看门狗、掉电保护、设备唯一密钥、温度监控、A/B 升级回滚、有线网络以及可选 Wi-Fi / 4G。普通开发板和 SD 卡仅用于验证，不作为长期批量交付方案。

### 5.3 传感器接入边界

- 板内低速器件：GPIO、I2C、SPI；
- 工业或较远距离：RS-485 / Modbus；
- 网络传感器：版本固定且有身份认证的 MQTT 或项目适配协议；
- 视频：USB、CSI 或 RTSP；
- 对外报警：统一 VisionGuard Event Protocol。

传感器不得绕过 Edge Detector 直接把私有格式扩散到云端。

### 5.4 独立联网与网关模式

开发板探测单元必须具备独立联网能力，不能依赖现场中控或另一台 PC 才能发送报警。基础网络能力为：

- 有线 Ethernet 作为商业设备的优先接入方式；
- Wi-Fi 作为常用可选接入；
- 4G / 蜂窝网络作为特定 SKU 或外接模块，不要求首个原型内置；
- 主动连接 VisionGuard Cloud，完成认证、配置同步、心跳、事件入库与可靠兜底、模型和 OTA；
- 所有公网报警、证据和管理流量统一通过 Server，不建立到 Receiver 的直连数据通道；
- 无互联网时继续本地检测、融合、联动和持久化排队，网络恢复后补发；
- 网络切换不改变设备身份、事件 ID 或逐接收端 ACK 状态。

同一套 Edge Runtime 支持三种角色；现有 Visual Detector 仍以 Detector 模式参与统一网络和管理体系：

| 模式 | 适用场景 | 行为 |
|---|---|---|
| Detector | 单个智能探测器 | 自己采集、判断、联网和发送事件 |
| Gateway | 多个轻量或第三方传感器 | 提供协议转换、身份代理、离线队列和现场网络出口 |
| Detector + Gateway | 首代推荐形态 | 自身完成视觉/融合检测，同时管理少量下游传感器 |

第一代不单独开发中控硬件。开发板以 `Detector + Gateway` 运行，需要时管理门磁、PIR、毫米波或 Modbus 设备；只有现场规模、接口数量或性能证明确有必要时，才拆分专用网关。

### 5.5 探测器管理代理

每台开发板探测器内置受控的 Management Agent，向 Web 控制台提供以下机器可验证能力：

- 首次配网、设备激活、证书和稳定 `deviceId`；
- 上报硬件、系统、传感器、模型和协议能力清单；
- 上报在线状态、温度、存储、内存、推理耗时、传感器健康、Server 连接质量和 outbox 积压；
- 接收带版本的目标配置，回报实际生效配置和失败原因，能够识别配置漂移；
- 管理传感器绑定、命名、区域、校准、启停、阈值和融合规则；
- 下载、校验、启用和回滚模型；
- 执行签名固件 OTA、A/B 切换和失败回滚；
- 导出脱敏诊断包和有限诊断动作，不默认提供无限制远程 Shell；
- 对配置、模型、固件、重启和恢复出厂等操作生成审计记录。

Management Agent 不参与具体 UI。它只实现稳定的设备管理协议，使 Android 接收端、Web 控制台和未来其他管理端使用同一套授权与状态语义。

## 6. Server Web 管理控制台

### 6.1 定位

Web 控制台是整套系统的最高权限管理中控，直接复用 Server 的账号、租户和设备权限体系。它负责完整的数据与设备管理，包括 Visual Detector、Edge Detector 和下游传感器；它不承担现场推理，Android Receiver 继续负责移动端即时报警。

个人或少量设备可以主要使用 Android Receiver，但系统所有者仍可通过 Web 控制台获得最高管理和调试能力。当前 Windows 检测端不扩张为中控，Win7 兼容端不承担商业设备管理职责。

### 6.2 管理范围

Web 控制台最终覆盖：

- **组织与场地**：租户、成员、角色、场地、区域和设备拓扑；
- **设备生命周期**：激活、配对、命名、移动、停用、替换、证书轮换和恢复出厂；
- **探测器管理**：能力清单、运行状态、传感器健康、推理性能、网络路径和本地队列；
- **传感器管理**：绑定、类型、端口、区域、校准、阈值、融合关系和故障状态；
- **配置管理**：单设备配置、场地模板、版本、下发结果、差异和回滚；
- **模型与 OTA**：版本清单、兼容约束、灰度、暂停、回滚和失败设备列表；
- **事件与处置**：报警查询、接收人送达状态、确认、备注和必要证据访问；
- **诊断与审计**：脱敏日志、网络测试、运行指标、操作审批和完整审计记录。
- **数据管理**：事件检索、证据查看、导出、删除、保留策略、备份状态、容量和数据完整性检查。

### 6.3 安全与操作边界

- 控制台最高权限不等于所有账号共享同一权限；至少区分系统管理员、租户所有者、运维人员和只读审计角色；
- 默认不保存原始连续视频；报警事件和必要证据按权限与保留策略进入 Server，控制台可在授权范围内查看和管理；
- 所有公网 HTTPS、WSS 和 Server 转发通信必须加密；Server 内部可以处理业务明文，但数据库、对象存储、备份、日志和导出文件仍需访问控制、最小暴露和必要的静态数据保护；
- 控制台登录、最高权限操作和紧急运维入口必须使用强认证、完整审计和会话保护；
- 配置和命令必须带目标版本、操作者、设备范围和幂等请求 ID；
- 批量升级、恢复出厂、密钥轮换等高影响操作需要二次确认、分批执行和停止开关；
- 控制台显示“期望配置”和“设备实际配置”，不能在下发请求成功后直接假定已经生效；
- 探测器离线时允许保存待执行配置，但必须显示尚未生效；
- 不把浏览器到设备的临时直连当作管理前提，所有管理状态经 Server 可审计控制面协调。

### 6.4 分步交付

Web 控制台不一次做成完整运营平台：

1. **最小控制台**：登录、场地、设备列表、详情、健康、单设备配置和审计；
2. **探测器控制台**：传感器拓扑、校准、融合规则、模型与 OTA；
3. **商业控制台**：成员角色、配置模板、批量灰度、事件处置、多场地和运营指标。

每一步都要求 Server API 和自动化权限测试先成立，再增加页面；不能让 Web 页面成为唯一实现业务规则的位置。

## 7. 数据传输与保留政策

### 7.1 概念

**传输政策**回答“什么数据何时进入 Server、失败时如何重试、如何送达 Receiver”。当前路线统一采用 Server 中心架构，不再保留 P2P 分支。

**保留政策**回答“数据保存在哪里、保存多久、谁能查看、何时删除、备份多久后消失”。报警文字、截图、连续视频、设备状态、调试日志和审计记录应分别制定，不使用同一个期限。

### 7.2 建议的传输政策

| 数据类型 | 主要路径 | Server 行为 |
|---|---|---|
| 报警事件与检测结果 | 探测器通过 WSS/HTTPS 立即上传 Server | 权威入库、去重、分发、记录逐接收端送达状态 |
| 报警截图与短证据 | 探测器可靠上传 Server | 按事件关联保存，供控制台和授权 Receiver 查看 |
| 设备离线与恢复 | Server 根据心跳状态机生成系统事件 | 按报警入库并送达 Receiver；恢复时关联关闭或更新活动报警 |
| 实时视频/实时画面 | 默认不进入首版；需要时由 Server 受控转发 | 不录制、不长期保存，单独实施并发、带宽、权限和限流 |
| 配置、命令、校准 | Server 控制面 | 记录期望值、设备实值、操作者、结果和审计日志 |
| 模型、固件与 OTA | HTTPS 下载 | 校验版本、目标硬件、签名、灰度和回滚状态 |
| 设备遥测与诊断 | 探测器定期批量上传 | 限频、聚合，异常时允许短期提高采样率 |

报警事件以 Server 成功持久化或 Receiver 成功持久化作为 ACK 条件，不能以“网络发送函数返回成功”当作已送达。探测器在收到满足策略的 ACK 前保留本地 outbox。

### 7.3 建议的首版保留政策

以下是产品默认值草案，不是法律固定期限；上线前应根据成本、客户合同和实际处理目的调整：

| 数据 | 免费版默认 | 付费版默认 | 说明 |
|---|---:|---:|---|
| 报警事件元数据 | 7 天 | 90 天 | 用户可主动删除；付费层可提供可配置期限 |
| 报警截图/短证据 | 72 小时 | 30 天 | 明显高于元数据的隐私与存储成本 |
| 连续视频 | 不保存 | 不保存 | 若以后提供录像，作为独立功能重新做容量与合规设计 |
| 原始设备遥测 | 7 天 | 30 天 | 更长周期只保留聚合指标 |
| 普通调试日志 | 7 天 | 30 天 | 默认脱敏，不记录密码、令牌和完整图片内容 |
| 安全与管理审计 | 90 天 | 180 天 | 包括登录、配置、导出、删除、升级和高权限操作 |
| 配置历史 | 最近 10 个版本 | 最近 30 个版本 | 支持回滚并限制无限增长 |
| 在线备份 | 主数据删除后最多 30 天自然过期 | 主数据删除后最多 30 天自然过期 | 删除期间停止其他处理并保持必要保护 |

控制台必须显示各类数据的当前保留规则、预计占用、删除结果和备份过期时间。用户删除与账号注销需要覆盖主数据库、对象存储、搜索索引和异步队列；备份无法立即逐条删除时，应在约定周期内自然过期且不得恢复用于正常业务。

中国大陆首发需遵循“为实现处理目的所必要的最短时间”原则，并向用户明确告知处理目的、数据种类、保存期限或确定方法以及删除和注销途径。具体合规文本和敏感信息判断在上线前由熟悉目标场景的专业人员复核。

## 8. 5.0 以内更新：视觉端驻留远控、硬件加速与四路检测

> 保存日期：2026-09-08。依据：owner 提供的 2026-09-07 计划图片，以及本次对话中的补充与纠正；对话确认优先于图片。
>
> 状态：实施中。2026-09-11 已将 WPF 主体调整为四个窗口来源，并形成四个独立 `WindowHandle` 的自动推理与隔离证据；第 8.6 节仍有动态视频、故障矩阵、完整报警链、持续运行和 owner UI 目检未完成。V1 协议与数据底座、V2/V3 主体实现已完成，V5/V6 已形成四来源主体闭环；V4 真实 Android 加速以及 V7 故障/持续运行/回滚验收尚未完成。下文未标记完成的技术选型、降级策略及工期是实施建议，不能描述为已交付能力。

**版本归属：本节全部工作纳入 5.0 以内（含 5.0）的更新范围，可在其间分批交付；具体小版本分配和发布日期尚未冻结。** 版本归属不表示已升级当前版本，也不授权版本同步或发布。

**当前实现进度（2026-09-09）**：已完成 V1 的来源身份最小闭环：`sourceId` 作为稳定身份、`sourceName` 作为事件名称快照，贯通 Server 校验、HTTP 入库、WS 广播、磁盘记录、历史响应和 Android Gson 兼容解析；`GET /api/alerts` 支持 `sourceId` 筛选。控制协议已增加接收端生成的 `requestId`、结构化 `forwarded`/`completed` 阶段、15 秒执行超时，以及最终回执仅返回原请求者；执行完成或超时的 requestId 在短期幂等窗口内拒绝重放，无效请求不会占用该 ID。Android 检测端、WPF 和 WinForms 均会原样回传请求 ID，旧检测端在单一无歧义待处理请求下由 Server 安全关联。检测端心跳还会声明经过 Server 白名单清洗的 `capabilities` 与 `components`，接收端兼容解析；当前只上报已经实现的控制、请求关联、截图和 WPF DirectML 能力，以及 `detectorApp=running`，不伪造尚未实现的驻留状态。独立截图消息由 Server 按已存报警记录和认证设备强制关联，并以权威记录补齐来源；设备不匹配、报警不存在、图片格式无效或备份关联失败时拒绝广播，接收端再次核对报警、设备和来源后才写入缓存。Server 真实 WebSocket 协议测试、两端 Android 单测、小米 15 安装启动检查和 Windows 两端 Release 构建及输出检查已通过。旧客户端不发送字段时继续按默认单来源及旧回执兼容。V1 的驻留组件状态聚合和完整来源 UI 仍未完成。

**V2 实现进度（2026-09-10）**：WPF 与 WinForms 已接入共用的当前用户会话命名互斥体，阻止手动启动与驻留远程启动产生第二实例。新增共用 Windows 驻留程序，以独立 `windows-resident` 身份连接 Server；Server 将其与检测端按设备聚合但保留独立连接表，并只把 `open-wpf`、`open-winforms`、`close-wpf`、`close-winforms` 四个生命周期命令路由给驻留。两种主程序通过当前用户会话事件上报运行握手并接收正常退出请求；启动完成以握手为准，打开后不自动监控。驻留提供显式登录启动启用/禁用命令，接收端设备卡根据组件状态显示两种程序的打开/关闭入口。正式发布脚本已把驻留四个运行文件合并进 WPF 与 WinForms ZIP，单端 Windows 构建也会构建驻留；驻留 Release 项目不再生成 PDB。Server 协议测试、Android 接收端单测与 Debug 编译、驻留及 Windows 两端 Release 构建已通过；本机进程级测试确认两种程序可握手和正常关闭。发布脚本语法检查通过，但因本轮未获版本同步/发布授权，尚未生成真正发行 ZIP；当前驻留程序基于 .NET 9，只覆盖受支持的现代 Windows，不能据此宣称 Win7 驻留远控兼容。Win7 SP1 x64 兼容已列为驻留后续交付硬门槛，但当前尚未实现或验收。登录后重启、网络中断、崩溃和完整远程 WSS 链路也未验收，因此 V2 仍属于“主体实现完成、完整验收待补”，不能标记为发布完成。

**V3 实现进度（2026-09-11）**：WPF 已提供持久化的 DirectML/CPU 后端选择，默认 DirectML；DirectML 初始化失败时显式回退 CPU 并保留原因，主窗口状态栏显示实际生效后端，设置页说明状态位置。RTX 2080 Ti 的单路、历史三图片并行和当前四窗口并行推理证据以及导航图标资源/发行布局检查均已通过；尚未覆盖其他 GPU、动态业务视频和 owner 的发行包目检。

**V3 后端一致性证据（2026-09-10）**：`tests/WpfInference.Benchmark` 在 DirectML 运行时会额外创建 CPU 参考会话，按解析后的类别、置信度和框 IoU 比较结果并将误差写入报告。本轮现有应用界面截图在 CPU/DirectML 均为 0 个有效检测，证明该负样本未出现后端特异误报；报告见 `artifacts/v3/wpf-directml-cpu-parity.json`。由于没有正目标，尚不能据此宣称召回率、框坐标或代表性精度验收完成；原始候选数组顺序不同也不直接作为语义失败。

**V5 多来源基础进度（2026-09-11）**：Server 心跳与接收端设备模型支持同设备最多四个来源；第五个来源截断，逐来源命令和参数调整只接受最近心跳中存在的 `targetSourceId`，无效来源不会占用 `requestId`。WPF 以 `default`、`signal-2`、`signal-3`、`signal-4` 四个稳定来源运行独立 `MonitorService`/ONNX Session/报警冷却实例，产品路径只保留 WindowHandle 与 ScreenRegion，不再提供 ImageFile 直读。主界面为 2×2 预览，每路的名称、窗口/区域、模型、类别、阈值、遮罩、FPS、冷却、启停、实际后端与错误独立持久化；旧单路设置首次迁移会备份并写入来源 1，所有来源保持停止。CPU 或 DirectML 回退 CPU 时拒绝第二路并行。`scripts/test-wpf-person-detection.ps1` 使用四个独立可见浏览器窗口并将已确认的固定句柄传给 smoke，验证真实 `WindowHandle` 捕获：每路至少 30 帧、每路 `person` 命中且实际 3 FPS，停止/配置隔离、移动缩放、遮挡、最小化故障隔离及恢复、关闭隔离、CPU 多路拒绝、DirectML 初始化回退和受控运行期故障全局停机均通过；报告见 `artifacts/e2e/20260912-170807/wpf-person-detection.json`。静态图片窗口不等于动态业务视频，完整报警链、持续运行、真实显卡驱动故障和 UI 视觉仍需验收。

**V5 故障处理进度（2026-09-12）**：WPF 窗口绑定新增标题、窗口类和进程名组合身份；标题变化时仅允许唯一稳定身份自动重绑，多候选明确拒绝。全黑 `PrintWindow` 结果已从调试日志升级为来源可见故障；DirectML 运行期推理异常会触发异步全局停机，避免多路静默转入 CPU，捕获故障仍保持单路隔离，FPS 只统计成功帧。窗口匹配、纯黑捕获、移动缩放、遮挡、最小化故障隔离与恢复、关闭隔离和受控 DirectML 运行期异常已经自动验证；显卡驱动重置或物理设备丢失仍需目标环境验收。

**V6 接收端进度（2026-09-11）**：设备页按父设备展示最多四个来源并提供能力受控的逐路启停和参数入口；WPF 心跳上报每路模型、冷却、阈值、类别和检测频率，接收端提交时携带对应 `targetSourceId`。父设备仅把 `isMonitoring && isReady && error` 为空的来源计为健康运行，因此四路中三路健康、一路未就绪或异常会显示“部分运行 3/4”，多来源继续禁用旧整机控制入口。Server 对命令白名单、目标能力、来源存在性和最终回执关联进行校验；无效请求不会登记待处理请求。警报筛选、逐来源通知静音、稳定身份和历史公平保留策略保持不变。每来源独立声音/时段、真机 UI 与完整告警链仍未验收，因此 V6 仍为部分完成。

**V4 Android 加速进度（2026-09-10）**：已删除把 `OrtSession` Java 类名误称为 Execution Provider 的日志，升级 Android 官方 Maven Mobile 依赖至 ONNX Runtime 1.29.0，新增 NNAPI 请求/实际 provider/回退原因的结构化状态及设置页展示，并在至少 3 次真实推理后解析 profile 写入硬件执行证据。原始 YOLO26 模型的 `Split` 形式会在小米 15 NNAPI session 创建时失败；新增 `scripts/prepare-android-nnapi-model.py`，将 12 个 `Split` 改写为 28 个等价 `Slice` 节点，发布准备阶段自动处理且保持逻辑文件名不变。归一化模型已通过 WPF 三张含人图片烟测，并在小米 15 真机完成 CameraX → 预处理 → NNAPI/CPU 分区推理，证据中的 `actualProvider=NnapiExecutionProvider`、`hardwareExecutionConfirmed=true`、provider 计数 `6/45`。原始模型启动仍按设计明确回退 CPU；QNN/NCNN 未集成，公网模型尚未因本地变更发布，完整检测端→Server→接收端报警链、持续运行和精度矩阵仍未验收，V4 不能标记完成。

本节是当前视觉端更新的执行路线，按 V0–V7 推进。所需的来源身份、控制回执和兼容迁移在 V1 内完成，并与第 9 节的统一协议方向衔接；完整多租户平台、Web 控制台及商业硬件规划不作为本节启动前提。第 9 节仍维护这些能力的演进与验收，不因本次整理自动分配到某个版本或标记为已完成。

### 8.1 已确认范围

| 模块 | 本次范围 |
|---|---|
| Windows 驻留 | WPF、WinForms 都支持；主程序关闭或崩溃后，仍能从 Android 接收端远程打开 |
| 启动语义 | 只打开主程序，检测保持停止，由 owner 手动启停；不自动恢复监控，不自动拉起崩溃进程 |
| 运行边界 | 首期保证 Windows 已登录、正常运行状态；锁屏、未登录、休眠等其他状态允许失败，但须如实显示结果 |
| WPF 加速 | 首先验证 RTX 2080 Ti；核显保留后续兼容扩展方向，首期不承诺未测试设备 |
| WinForms 推理 | 本次增加驻留远控，保持现有推理及 Win7 兼容范围，不随之迁移 YOLO26 或现代 GPU 运行时 |
| Android 加速 | 仅 Android 检测端；小米 15 首期实现至少一种真实生效的 GPU 或 NPU 加速，NPU 不是硬性要求 |
| WPF 多路 | 最多四个窗口来源；只有硬件加速实际生效时才能同时运行多路；目标每路至少 3 次/秒 |
| 每路配置 | 模型、检测类别、阈值、遮罩、检测间隔、报警冷却及启停独立 |
| 接收端 | 同设备的多来源归为一组，支持按来源筛选历史、独立通知设置及逐路远程控制 |
| 采集扩展 | 首期使用现有窗口采集，预留 USB 摄像头及采集设备接口，不同时实现这些新来源 |
| 图标 | 修复 WPF 导航图标丢失，并验证真实发行包 |
| 交付原则 | 先低成本技术验证，再分批交付；每批 UI 改动由 owner 在实际设备上目检 |

### 8.2 可行性与已发现的改造点

总体可行。主要工程量在来源状态隔离、跨端协议、窗口采集稳定性和加速真实性验证，不是简单增加 GPU 开关或复制四套界面。

- 两端退出都会释放控制连接，必须由独立驻留组件接收远程启动请求；当前业务级暂停/恢复不能启动已退出的进程。
- WPF 已接入 DirectML/CPU 后端选择，并实现最多四个独立窗口配置、引擎、定时器与报警冷却的来源协调器；停止时等待执行中 Tick 后再释放 Session，CPU 多来源由运行层拒绝。真实窗口人员捕获和逐路 3 FPS 已自动验证，动态窗口重定位和故障矩阵仍需补验。
- 窗口采集包含同步 `PrintWindow`，黑屏、最小化、权限差异及目标窗口阻塞需要实测；不能把捕获失败当成无目标。
- 窗口重定位仅靠标题首个匹配不适用于多路及远程重新打开，需要处理重名、标题变化和绑定歧义。
- Server 当前连接身份会让相同角色和设备 ID 的新连接替换旧连接，驻留不能直接冒充主程序连接。
- 控制回执目前存在“已转发即 success”的语义，缺请求关联；执行结果不能仅按命令名关联，也不能无差别广播给所有接收端。
- 报警、历史及设备状态目前以设备为单位，新增来源必须覆盖 WS、HTTP 校验、持久化、历史响应、缓存、通知和截图关联，不能仅增加实时消息字段。
- Android 当前未包含 Qualcomm QNN/NCNN；已实现并验证 ONNX Runtime NNAPI 路径，已移除将 Java 类名作为 Execution Provider 证据的误导日志，并显式报告 CPU 回退原因。真实 NNAPI 证据仍依赖归一化模型、目标设备 profile 和持续运行/精度验收。
- WPF 导航 PNG 曾作为外部 Content 输出，而发布脚本排除 Assets，导致发行布局丢失图标。2026-09-08 已从项目资源声明根源修复：三个导航 PNG 改为内嵌 WPF Resource，未引用 PNG 不进入输出；Release 构建、程序集资源及模拟正式排除规则的压缩包检查通过，仍待 owner 在真实发行包中目检。

审查入口：

- [WPF 监控服务](../../detector/windows-wpf/Services/MonitorService.cs)、[WPF 项目文件](../../detector/windows-wpf/VisionGuard.csproj)、[WPF 导航](../../detector/windows-wpf/Views/MainWindow.xaml)。
- [Server 连接与命令路由](../../server/src/services/ConnectionManager.ts)、[远程配置校验](../../server/src/services/ControlProtocol.ts)。
- [Android 推理引擎](../../detector/android/app/src/main/java/com/xgwnje/visionguard/inference/OnnxInferenceEngine.kt)、[发布入口](../../scripts/publish-release.ps1)。

**V2 打包烟测（2026-09-10）**：在不改版本、不写发布元数据和不上线的前提下，本地组合 WPF 与驻留 Release 输出，生成 `artifacts/v2/wpf-resident-package-smoke.zip`；共 11 个条目，驻留四个运行文件齐全，且未发现 PDB、LIB、DLL.config、ONNX、Assets 或 alerts。它证明当前组合规则可产出干净 ZIP，但不替代正式版本发布流程验收。

### 8.3 推理后端与设备支持策略

WPF 建议先验证 ONNX Runtime DirectML，CUDA 作为性能或兼容性不达标时的候选。DirectML 可沿用 ONNX/C# 并为核显扩展保留空间，但已处于持续维护阶段，新功能转向 WinML；推理业务必须通过后端接口隔离。CUDA 需要另外验证 ORT、CUDA、cuDNN 版本及发行依赖，不能盲目升级。

Android 建议先验证 QNN/Hexagon NPU；小米 15 的骁龙 8 Elite 对应 HTP v79。若量化精度、集成或持续运行不达标，再验证 NCNN Vulkan GPU。首期只需交付其中一种合格加速，不同时铺开多种未经验证的后端。

2026-09-08 核验的官方 QNN 代表性移动平台如下，属于候选清单，不是 VG 已支持清单：

| SoC | HTP 目标 | 本次安排 |
|---|---|---|
| 骁龙 888 | v68 | 未适配前 CPU |
| 骁龙 8 Gen 1 / 8+ Gen 1 | v69 | 未适配前 CPU |
| 骁龙 8 Gen 2 | v73 | 后续候选，未适配前 CPU |
| 骁龙 8 Gen 3 | v75 | 后续候选，未适配前 CPU |
| 骁龙 8 Elite | v79 | 小米 15 首期验证 |
| 骁龙 8 Elite Gen 5 | v81 | 后续候选，未适配前 CPU |

不能从系列名称推断所有骁龙 7/8 系列都可用，也不对其他 SoC 家族默认开放加速。设备匹配只是资格检查，随后必须通过运行库加载、模型加载、真实推理和实际执行后端确认。支持规则应记录设备/SoC、系统与驱动、运行库、模型变体及验证结果。

设置页区分 CPU 运行、正在验证、GPU 已启用、NPU 已启用、加速失败并回退 CPU；显示当前硬件、可用类型及失败原因。初次验证，以及驱动、模型或运行库变化后重新检查。部分计算回退 CPU 时诊断信息必须如实记录，不能以发现 GPU、加载库成功或速度变快作为唯一证据。

模型清单和缓存增加后端、输入尺寸、精度、目标架构及校验信息。QNN 专用模型不能直接作为 CPU 回退模型，继续保留 CPU ONNX 下载路径；新模型变体同样按需下载，不打包进入客户端。

RTX 2080 Ti 四路各 3 次/秒是验证目标，不是已证明的任意模型组合性能。要同时检查采集、预处理、推理、报警和 UI 开销；不以总计 12 次/秒代替每路达标。

### 8.4 设备、来源与控制协议

采用设备 → 来源两层结构：同一检测设备是父组，WPF 目标最多四个来源；来源使用持久 `sourceId`，不以显示名称作为身份。WinForms、Android 检测端、旧客户端及旧历史映射到默认单来源，保留原 `deviceId` 和 `alertId`。

| 对象 | 职责 |
|---|---|
| 设备 | 设备身份、驻留状态、主程序状态、硬件与控制能力 |
| 来源 | 窗口绑定、检测配置、运行状态、独立冷却 |
| 报警事件 | 事件 ID、设备 ID、来源 ID、事件发生时的来源名称快照 |
| 控制请求 | 请求 ID、目标设备/来源、作用域、操作、有效期和执行结果 |

驻留组件使用独立组件身份与连接表，同主程序归入同设备。能力声明区分应用级启动和来源级启停，不复用现有 resume 表达进程启动。

请求增加 `requestId`、目标来源、有效期、去重与幂等处理。回执区分接收/转发、执行完成、失败及超时，按请求关联返回；启动成功以主程序完成握手为准，不以创建进程或 Server 转发成功为准。保留授权检查，仅向相应请求者返回结果或使用明确定义的状态订阅。

历史增加设备和来源联合筛选；来源改名不改写旧事件名称，删除来源不删除其历史身份，新建同名来源不混入旧记录。截图继续按唯一事件 ID 关联，延迟补图也必须归入正确来源。检查历史保留配额，避免高频来源无意挤掉其他来源的全部历史。

Server 先提供兼容旧字段的读写与能力协商，再升级接收端和检测端；不支持来源控制的旧版本不能误把逐路命令执行为整个设备的命令。此计划沿用第 3 节的 Server 中心传输和统一管理方向，不另建临时公网管理通道。

### 8.5 驻留远控行为

建议采用用户登录会话中的独立后台程序：启用后随用户登录启动，主程序退出不影响它；首期不由 Windows 系统服务直接承担窗口采集。后台保持 WSS，通过受限本地通信与主程序交互，只接受固定 VG 操作，不接受任意路径、Shell 或脚本。

| 情形 | 行为 |
|---|---|
| 主程序关闭或崩溃 | 上报状态，保持停止，不自动拉起 |
| 远程打开 | 打开指定 WPF 或 WinForms；所有检测来源保持停止，等待手动启停 |
| 主程序已运行 | 返回已运行，不创建第二实例，也不修改监控状态 |
| 远程关闭 | 请求正常退出；驻留保持在线；退出失败如实报告 |
| 驻留禁用 | 关闭驻留连接、取消登录启动，并说明失去远程打开能力 |
| 不符合首期运行边界 | 如实报告失败或不可用，不自动改设备设置或解锁 |

接收端区分驻留在线、主程序运行、主程序响应、各来源监控状态。两种 Windows 客户端共用驻留实现，但区分应用类型和受控安装路径，加入单实例处理，防止用户启动和远程启动竞争。驻留配置独立保存，不直接与主程序并发写同一设置文件。

### 8.6 四路检测、界面与降级（已批准计划）

每个来源拥有独立配置、监控状态、模型和冷却，公共调度器公平分配资源。每路只保留最新待处理画面，防止旧帧排队；同路禁止重入，同一推理 Session 的并发规则服从后端限制。停止和切换模型时，可靠等待正在执行的任务结束后再释放资源。

通过采集接口预留其他设备来源，首期实现窗口采集。窗口重定位结合应用身份、窗口类别和标题规则；无匹配或有歧义时保持停止并提示重新绑定。一路窗口阻塞不得拖住其他来源；采集失败、黑屏和超时必须显式报告。

CPU 禁止多路由运行层强制执行，不能仅禁用 UI 按钮。建议 GPU 运行中失败时暂停多路、上报原因并保留配置，由 owner 选择一路手动以 CPU 启动，不自动挑选来源继续监控。若模型组合不达标，先优化并报告瓶颈；不能静默降低频率或替换小模型，改变已确认目标须再由 owner 决定。

3 次/秒是性能能力基线；owner 主动延长某路检测间隔时，该路按配置验收。

#### 8.6.1 最终布局与操作

2026-09-11 owner 已审核通过。本节替代此前三路界面方案；四路主体实现与测试工具已按本节落地，未完成的动态视频、故障矩阵、持续运行、完整报警链和 UI 目检继续受本节约束。

采用与应用图标一致的现代简约风格：近黑与暖灰构成主体，白色承载信息，品牌红只用于选中态和关键操作，运行状态另用克制的语义色。固定四个信号槽位，可只配置其中一路或多路。2026-09-11 owner 要求移除左侧导航、页内重复品牌块和信号区说明栏，并合并重复职责：中部只保留 2×2 等大实时预览，右侧成为唯一配置入口，以“当前信号 / 运行环境 / 连接”切换逐路配置、共享推理资源和设备连接；检测类别使用下拉多选菜单，运行汇总与整机启停位于顶部，不占用第四个画面。当前来源名称与状态共用一行，名称点击后原位编辑并在回车或失焦时自动保存；其余配置遵循“采集目标 → 识别设置 → 检测参数 → 区域排除 → 保存”的操作流，区块只以标题和合理间距分组，避免嵌套卡片增加视觉负担。

```text
┌───────────────────────────────┬──────────────────┐
│ 运行路数 / 连接 / 异常 / 整机启停               │
├───────────────┬───────────────┤ 右侧控制台       │
│ 信号 1        │ 信号 2        │ 当前信号         │
├───────────────┼───────────────┤ 运行环境         │
│ 信号 3        │ 信号 4        │ 连接             │
│               │               │ 保存 / 取消      │
└───────────────┴───────────────┴──────────────────┘
```

- 每张卡片独立显示名称、画面、检测框、状态、更新时间、推理耗时、最后报警、实际后端及启停；不保留重复的全局底部信息栏。点击选中后更新右侧设置，切换不影响其他路检测。
- 顶部显示运行路数、服务器连接、最近报警和异常摘要，提供“启动已配置来源”“全部停止”。批量启动逐路反馈结果，CPU 回退时停止继续启动，不自动挑选一路继续运行。
- 默认窗口约 1420×880；缩小时保留四路概览，右侧控制台可滚动。控制台采用高信息密度布局：主要输入与操作保持统一的 40px 高，只有标题旁的次要清除操作缩为 28px；同类字段和动作优先并排，移除嵌套卡片、重复页标题与说明文字，并用区块间距维持层级。预览区压缩非交互留白、卡片标题栏和启停区，把主要空间让给画面，但不缩小必要点击区域。状态同时使用文字与颜色，停止或异常时，最后画面明确标记状态与时间，不能冒充实时画面。
- 每路支持选择窗口、窗口内选区及原有屏幕区域捕获；不保留额外的第五条单路运行通道。名称、目标、遮罩、模型、检测类别、阈值、频率和冷却独立保存；右侧“运行环境”只负责共享后端和模型下载，不再重复显示逐路阈值、类别、频率或冷却。
- 配置使用“保存 / 取消”。运行时锁定该路配置，提示先停止该路；其他路继续运行。换采集目标或改选区时，明确提示并清除不适用的遮罩。

#### 8.6.2 运行接口与迁移

- 将图片专用来源 ViewModel 改为通用信号来源模型，复用窗口选择、选区、遮罩和检测服务；每路独立持有预览、检测结果、最近成功时间和窗口绑定信息，禁止多路覆盖同一预览字段。
- 状态区分未配置、就绪、启动中、运行中、已停止和异常。FPS 只统计有效检测帧，并随时间衰减；全黑画面提示“疑似黑屏”，不直接判定为无人。一路窗口关闭或捕获失败不能拖住其他三路。
- 沿用持久 sourceId 与 targetSourceId。WPF 运行上限、Server 数量校验、接收端来源列表和契约测试同步支持四路；已配置但失联的来源继续上报错误，不能从列表消失。本次接收端仅适配四路，不扩展为独立 UI 重设计。
- 原正式单路配置迁入第一路，保留 default 身份；第二至第四路使用持久身份。旧图片测试来源退役，历史不冒充新窗口来源；迁移保留原配置备份，启动后保持停止。改名不改变来源身份。
- 无来源目标的旧远控命令仅操作第一路；带来源目标的命令严格逐路执行。本地与远控共用运行约束，CPU 只允许单路；GPU 失败处理遵循本节降级规则，不允许静默多路 CPU。

#### 8.6.3 测试工具替换

- 完全移除图片直读测试：产品图片测试入口、ImageFile 捕获分支及专用 smoke 工具均列入待删除范围。删除前核对引用及历史价值，将来源隔离、CPU 多路拒绝和后端失败等有效断言迁入运行约束测试及真实窗口验证入口。
- 日常验证由浏览器打开四张不同的本地含人图片，每张一个独立窗口；四个标签页不算四个窗口。不新建图片播放器或浏览器管理器。
- 实施时同步替换旧脚本调用、README、AGENTS.md、项目 Skill、运维文档及文档检查规则中的旧入口。历史证据留在验证报告，工具实际退役后再标注退役状态；不提前删除仍可用的操作说明。

#### 8.6.4 分批交付与通过条件

1. 先用四个独立浏览器窗口验证现有捕获方式取得真实画面。出现黑屏先定位捕获兼容性问题，再进行大规模改造；不使用图片直读绕过失败。
2. 交付 2×2 四路界面、逐路选择和配置交互，由 owner 在实际 Windows 桌面目检；确认后接通完整运行行为。
3. 四路分别显示正确图片与检测框，各至少命中一次 person。将一路换成无人画面再恢复，确认画面和检测结果确实更新。
4. 覆盖单路停止、换窗口、修改模型/阈值/遮罩，其他三路持续有效检测；验证关闭、重绑、移动、缩放、遮挡、最小化，以及 CPU 限制、GPU 失败、远控目标隔离和迁移后不自动监控。
5. 运行相关契约测试、WPF Release 构建与产物检查，以及文档检查。四路 GPU 性能重新实测，按第 8.9 节逐路验收，不沿用三路结果、不以总吞吐代替逐路达标。

浏览器静态图片只证明真实窗口采集、人员检测与四路操作，不等同于动态业务视频、持续运行或完整检测端→Server→接收端报警链验收。未覆盖项与失败如实记入验证报告。保留 owner 已有改动；不自动改版本、提交或发布。

### 8.7 接收端体验

设备页使用可展开父组，组内显示来源名称、状态、模型、实际检测频率及异常。组状态聚合为全部运行/部分运行/全部停止等；保留现有设备排序和离线管理规则，不能把原有在线状态分组误当物理设备组。

逐路启停与配置操作有独立执行反馈。历史提供设备及来源筛选。通知偏好建议保存在当前接收端，以设备和来源为键，支持继承默认值或独立声音、振动和静音；系统通知渠道行为需要按 Android 约束验证。静音只影响提示，不停止检测或历史收录；四路报警不能被一个全局冷却互相压制。

### 8.8 分阶段交付

以下 V0–V7 均属于本节的 5.0 以内更新，表示开发依赖与验收顺序，不对应具体发布版本号。第 9 节的平台阶段 0–6 使用独立编号，相关底座能力应复用本节成果，避免重复建设。

| 阶段 | 工作内容 | 交付与通过条件 |
|---|---|---|
| V0 技术验证 | CPU 基线；RTX 2080 Ti 单路/四路原型；小米 15 QNN 原型；驻留打开两种客户端原型；图标复现 | 性能报告、实际后端执行证据、检测结果对照、依赖清单；主路径证明可行后才大规模改造 |
| V1 协议与数据 | 来源身份、能力声明、组件状态、请求关联、执行回执、历史持久化与兼容 | 新旧单路继续可用；并发控制不串目标；历史和截图不丢来源 |
| V2 Windows 驻留 | 共用后台、两端适配、开关、远程打开/关闭、单实例、本地通信、Win7 SP1 x64 兼容 | 关闭或杀掉主程序后可远程打开；打开后不监控；重复启动不产生多实例；在 Win7 SP1 x64 上完成安装/启动、WSS/TLS、进程握手、远程开关、网络恢复和正常退出验收 |
| V3 WPF 加速与图标 | 后端接口、GPU 选择、真实状态、CPU 回退、资源修复 | RTX 2080 Ti 真正执行推理；失败明确显示 CPU；干净目录解压发行包图标正常 |
| V4 Android 加速 | NNAPI 集成，必要时继续验证专用 GPU/NPU；模型兼容准备、设备规则、状态 UI | 小米 15 完整检测报警链通过，持续运行与精度合格；未适配设备稳定用 CPU |
| V5 WPF 四路 | 独立运行状态、配置、模型、冷却、公平调度、窗口绑定、2×2 四路 UI；移除图片直读测试 | 四路各达约定频率；一路卡顿、停止或改配置不影响其他路；按第 8.6 节分批目检 |
| V6 接收端体验 | 父组与来源卡片、逐路控制、历史筛选、独立通知 | 静音一路不影响其他路，筛选准确，组状态与实际状态一致 |
| V7 整体验收 | 持续运行、网络/GPU/进程故障、迁移、发行包和回滚验证 | 全链路验收通过，形成可回滚产物；版本和发布须另有明确授权 |

V0 RTX 2080 Ti 子项验证记录（2026-09-09）：WPF 改用 ONNX Runtime DirectML 1.19.0，并保留初始化失败时的显式 CPU 回退及原因。`yolo26n_320` 单路 100 次推理平均约 2.87 ms；三路独立 DirectML 会话、100 次/路的纯推理平均约 8.4 ms/路。该轮三路输入是界面截图负样本，未验证人员召回；机器可读报告位于 `artifacts/v0/wpf-directml-3image-yolo26n320.json`。该结果证明 320 输入的三路计算链路原型可行，不等价于真实窗口采集、人员检测、报警、UI 总链路或 640/混合模型矩阵已经验收；ONNX Runtime 对少量形状运算保留 CPU 分配，不能描述为所有节点 100% 在 GPU 上执行。

依赖顺序：V0 → V1 → V2；V3、V4 在接口与协议稳定后可独立推进；V5 依赖 V1 与 V3；V6 随 V1/V2/V5 分批联调，不等最后才接入；V7 汇总验收。可独立工作使用职责和文件范围明确的子智能体，主线程负责整合及验证。

首批只交付四类验证结果：RTX 2080 Ti 四路报告（须重新实测）、小米 15 实际加速报告、两端远程打开且不自动监控的原型、发行包图标复现与修复验证。

### 8.9 验收清单

| 领域 | 验收要求 |
|---|---|
| 四路基线 | RTX 2080 Ti、四个真实目标窗口、各路配置 3 次/秒；预热后连续运行，建议每路滚动 10 秒至少完成 30 次有效检测 |
| 时效 | 记录帧龄、实际间隔和 P95/P99 延迟；不能靠处理积压旧帧达标；V0 根据真实样本冻结延迟及精度阈值 |
| 模型矩阵 | 覆盖同模型四路、不同模型混合、320/640 输入及资源压力组合；注明硬件、驱动、版本和样本，不能推广未测组合 |
| 精度 | CPU 与加速后端对同一代表性样本比较检测结果，检查量化后的召回、误报和坐标/遮罩一致性；不以速度代替质量 |
| 来源隔离 | 改阈值、遮罩、模型、冷却、启停和窗口失联不串扰；一路异常不饿死其他路 |
| 加速真实性 | 有实际后端执行/模型计算分配证据；模拟运行库、模型或 GPU 失败，UI 和运行模式一致 |
| CPU 限制 | 本地、远控、重启和故障回退路径均不能同时运行多路 CPU |
| 驻留 | 正常关闭、崩溃、重复启动、驻留禁用、网络断开、启动失败和 Win7 SP1 x64 兼容均有准确结果；进程打开后不自动监控 |
| 控制 | 并发请求、重复/过期命令、错误目标、离线与超时不串结果；转发不等于执行完成 |
| 历史与通知 | 来源改名/删除/重建不串历史；延迟截图正确归属；静音不丢记录，不影响其他来源 |
| 兼容 | WinForms 与 Windows 驻留的 Win7 SP1 x64 兼容性分别验收；旧单路设备及旧事件可读；新命令不误投旧设备；升级后配置保留，回滚可诊断 |
| 稳定性 | 先通过 1 小时测试，再做 24 小时持续运行和故障恢复；手机同时检查温升、降频、资源增长及后台检测链 |
| 发行与 UI | 受影响端按项目 skill 构建，检查输出目录与真正发行包；干净目录运行；每批 UI 由 owner 实机目检 |

### 8.10 工期估算与文档维护

按一名主开发者配合 AI、两台目标设备可持续测试估算；这是原型验证前的范围估计，V0 后重新收敛，不是固定上线日期。

| 工作包 | 工作日 |
|---|---:|
| 技术验证 | 4–7 |
| 协议与数据兼容 | 4–7 |
| Windows 驻留与两端接入 | 5–8 |
| WPF 加速与图标 | 3–5 |
| Android 首种加速 | 5–10 |
| WPF 四路与接收端体验 | 待四窗口低成本验证后重估（原三路估算 10–16） |
| 回归、迁移与发行验收 | 5–8 |
| 合计 | 待四路范围重新估算（原三路范围 36–61） |

原三路范围约 7–12 个工作周，不能直接作为四路承诺；Android 后端集成和窗口采集兼容性是最大变数。实施进展与范围调整更新本节，当前实现事实更新对应专题文档；不把未来功能写成 CHANGELOG 中的已交付能力。入口/命令/依赖变化时同步 AGENTS.md 与 README，运行 `node scripts/check-docs.js`。不自动变更版本、提交或发布。

### 8.11 官方资料（核验日期：2026-09-08）

- [Microsoft：Interactive Services](https://learn.microsoft.com/en-us/windows/win32/services/interactive-services)：服务与用户桌面的边界。
- [ONNX Runtime：DirectML](https://onnxruntime.ai/docs/execution-providers/DirectML-ExecutionProvider.html)：硬件要求、维护状态、Session 并发限制。
- [ONNX Runtime：CUDA](https://onnxruntime.ai/docs/execution-providers/CUDA-ExecutionProvider.html)：后端及 CUDA/cuDNN 依赖。
- [小米 15 官方规格](https://www.mi.com/global/product/xiaomi-15/specs/)：骁龙 8 Elite。
- [Ultralytics：QNN](https://docs.ultralytics.com/integrations/qnn)：YOLO26 导出、量化、HTP 目标及平台代表清单。
- [ONNX Runtime：QNN](https://onnxruntime.ai/docs/execution-providers/QNN-ExecutionProvider.html)：Android 集成、后端配置与 profiling。
- [Ultralytics：NCNN](https://docs.ultralytics.com/integrations/ncnn)：YOLO26 导出及 Vulkan GPU 路径。

上述资料证明存在部署路径，不证明当前 VG 已支持相应设备；实施时固定实际依赖版本并重新核对兼容性。

## 9. 平台与硬件演进路线

本节保留平台和硬件方向的阶段 0–6，描述能力依赖及验收条件；阶段编号不等于版本号。当前执行优先级见第 11 节，已确认的 5.0 以内视觉更新见第 8 节。

阶段 1 的现有端可靠性工作与第 8 节共享回归证据；阶段 2 复用第 8 节形成的设备/来源身份、能力声明及控制请求关联，再扩展账号租户、可靠投递与 Web 管理。硬件场景选择约束定制硬件和多传感器开发，不阻塞已确认的视觉端更新。

### 阶段 0：产品、市场与数据政策闸门

目标：在大规模重构前确定免费/付费边界、中国大陆首发要求、首个硬件使用场景和失败代价。

交付：

- 固化免费 Visual Detector 层与付费 Edge Detector 层的产品边界；
- 为付费版硬件探测器选择一个首发使用场景，例如无人值守空间入侵、仓库门禁或人员危险接近；
- 以漏报风险为最高优先级定义召回率、故障覆盖、报警延迟和最大无感失联时间；允许一定误报，但必须定义误报上限、静音和复盘机制；
- 建立真实环境样本和事件回放集；
- 明确各类数据的传输、保存、查看、导出、删除、注销和备份过期政策；
- 明确中国大陆首发所需的个人信息告知、同意、最小必要、安全保护和事件处置清单。
- 完成依赖库、模型、数据集、素材和构建产物的许可证清单，确认商业分发兼容性并生成必要归属声明。

闸门：没有明确场景、样本和量化指标时，不进入定制硬件和大规模多传感器开发。

### 阶段 1：4.x 商业可靠性基线

目标：在新架构研发期间保持现有产品可用，并提炼统一协议所需证据。

交付：

- 记录当前报警生成、截图、WS 发送、Server 持久化和 Android 展示的端到端语义；
- 为事件 ID、重试、重复报警、截图缺失、设备离线报警、恢复事件和网络恢复建立确定性测试；
- 形成故障码、脱敏日志和基本运行指标；
- 明确未来 Web 控制台所需的设备状态、配置版本和审计字段，但不在 4.x 内复制一套临时管理协议；
- 保持版本、模型下载、更新与现有发布链路稳定。

闸门：关键报警链路能够自动复现，修改前后可机器判定，不依赖只看 UI 猜结果。

### 阶段 2：统一身份、事件与可靠投递

目标：建立所有未来端都能使用的 Server 中心商业网络底座，不引入 P2P 多路径复杂度。

交付：

- 账号、租户、设备身份和能力声明；
- 版本化 Event Protocol、公网加密传输和跨平台测试向量；
- 探测端持久化 outbox、接收端幂等 inbox、逐接收设备 ACK；
- HTTPS/WSS `443` 可靠中继、权威事件存储和短期离线转发；
- Server 基于心跳生成设备离线报警和恢复事件，并纳入标准事件、ACK、重试和审计；
- Android 系统推送唤醒与前台 WSS 协同；
- Win7 / WinForms 继续以 TLS 1.2 可靠兜底链路运行；
- 提供最小 Server Web 控制台：场地、设备列表、设备详情、健康状态、单设备配置和审计记录。

闸门：设备失联能在策略阈值内生成一次离线报警，恢复时生成关联恢复事件；接收端离线、Server 重启和重复投递情况下不丢报警、不重复提示；跨租户访问自动化测试全部拒绝；控制台不能越权查看或修改其他租户设备，并能区分配置“已下发”“待设备接收”“已生效”和“失败”。

### 阶段 3：Server 高可用与多通道报警

目标：在统一 Server 路径上提升可用性、送达率和可观察性，使设备离线、Receiver 离线和 Server 自身异常都能被不同层次感知。

交付：

- WSS/HTTPS 连接状态机、退避重连、网络切换、outbox 补发和可观察状态；
- Server 离线判定、防抖、单次失联去重、恢复关联和逐 Receiver 送达状态；
- Android 前台 WSS 与适合中国大陆终端的系统推送协同；
- Server 带宽、连接数、队列积压、存储、限流、配额和滥用防护；
- 独立外部健康监测、备份恢复演练和必要的服务冗余，防止业务 Server 自监控失效；
- Win7 SP1 x64 Visual Detector 的 TLS 1.2、长连接、休眠恢复和断线补发验证，以及 Android 真机、家庭网络、移动网络和企业网络测试矩阵。

闸门：拔网线、断电、杀进程和现场路由故障都能在规定时间内形成可追踪的设备离线报警；恢复后不重复旧报警且能记录失联时长；业务 Server 故障能由独立监控通道发现；容量压测达到目标设备数。Win7 兼容端必须通过握手、长连接、休眠恢复、退出清理和发行目录检查。

### 阶段 4：Detector SDK 与开发板原型

目标：把产品核心从视觉客户端提升为可扩展探测平台。

交付：

- Sensor Adapter、Inference Engine、Fusion Engine、Event Engine、Outbox、Transport Manager 和 OTA Manager 边界；
- Linux ARM64 Edge Detector 原型；
- 摄像头 + 毫米波/PIR + 门磁的最小组合；
- Ethernet 独立联网、Wi-Fi 可选联网，以及 Detector + Gateway 双模式；
- 统一 Observation、AlertEvent、设备健康和能力清单；
- 传感器断开、异常值、时间漂移、推理超时和降级策略；
- Management Agent 与 Web 控制台联调；
- 远程配置、模型更新、日志导出和失败回滚。

闸门：72 小时稳定运行；断网和异常断电后事件不损坏；不依赖现场 PC 或专用中控即可完成远程报警；两传感器融合相对单视觉优先改善漏报指标，误报处于可管理上限内；传感器、推理、进程或网络故障不会静默形成探测盲区；控制台能准确显示能力、配置、健康、Server 连接和 outbox 状态。

### 阶段 5：首个垂直场景试点

目标：证明客户价值，而不是继续堆积通用能力。

交付：

- 小规模真实场地部署；
- 安装、校准、网络诊断和设备替换流程；
- 误报/漏报复盘与带版本的数据闭环；
- 多接收人、值班和事件处置记录；
- Web 控制台的传感器拓扑、校准、融合规则、模型与 OTA 进入试点；
- 存储、带宽、推送和人工支持成本测量。

闸门：达到阶段 0 定义的业务指标，并证明单设备交付与持续服务成本可接受。

### 阶段 6：设备规模化与商业运营

目标：在试点成立后建设批量交付能力。

交付：

- 工业 SOM 或定制载板评估；
- 工厂烧录、设备证书、批量注册和密钥轮换；
- A/B OTA、灰度、回滚和固件物料清单；
- 组织、成员、角色、审计、配额和商业套餐；
- 免费版纯软件视觉方案的云资源配额、付费版硬件探测器的服务权益和续费策略；
- Web 控制台的场地模板、批量配置、灰度升级、审批和运营指标；
- 多区域服务接入、容量规划、备份恢复和服务等级指标。

闸门：批次可追溯、升级失败可恢复、租户隔离可审计、容量与成本可以按设备数量预测。

## 10. 暂不进入主线的事项

- 重新引入 P2P、ICE、STUN、TURN 或浏览器到设备直连，除非未来有经过成本与场景验证、Server 转发无法满足的明确需求；
- 在阶段 0 场景未确定前，同时支持大量传感器；
- 第一代原型即自研完整主板；
- 第一代原型同时开发独立中控硬件；
- 让开发板探测器依赖现场 PC 或中控才能联网报警；
- 为追求 Win7 兼容而限制 Server、Web 控制台、Android、WPF、Linux Edge Detector 或未来硬件技术栈；
- 让每种传感器直接定义 Server API 和接收端页面；
- 把无限制远程 Shell 当成 Web 控制台的日常管理能力；
- 在缺少真实环境数据时用局域网演示代替商业可用性结论；
- 把未来规划写进 CHANGELOG 或描述成当前已交付能力。

## 11. 当前执行顺序与后续规划

### 11.1 当前执行：5.0 以内视觉更新

以[第 8 节](#8-50-以内更新视觉端驻留远控硬件加速与四路检测)为执行入口，先完成 V0 的四类技术验证，再根据结果推进 V1–V7。具体阶段、验收清单和工期只在第 8 节维护。

本轮仅明确版本范围和执行顺序；每批通过验证后再确定实际发行版本，不自动修改 `VERSION`、提交或发布。

### 11.2 后续规划：平台与商业硬件

产品定位、商业分层和兼容边界见第 1–2 节；传输及离线事件方向见第 3–4 节；Web 管理权限见第 6 节。后续依照第 9 节推进前，需完成：

1. 为付费版硬件探测器选择首个具体使用场景；
2. 根据该场景确定第一代开发板的一个触发传感器和一个确认传感器；
3. 审核并调整首版保留政策默认值；
4. 按首发场景确定设备离线阈值、误报抑制方式和 Receiver 系统推送通道；
5. 为第 9 节阶段 1、阶段 2 建立事件协议、Management Agent、数据管理和最小 Web 控制台的验收清单，并复用第 8 节已验证的协议与管理能力。

这些事项用于冻结后续平台与商业硬件的交付范围、硬件清单和实施排期，不作为第 8 节视觉更新的前置条件；其具体版本归属另行确定。
