# VisionGuard v4.4.4

本版本更新 VisionGuard 全端品牌图标。

- Windows WinForms 与 WPF 检测端使用统一的黑底红白折带 V 图标。
- Android 检测端使用白底黑红图标。
- Android 接收端使用红底白黑图标，强化报警端辨识度。
- 补齐 Android legacy、round、adaptive、monochrome 与 Play Store 尺寸资源。
- 图标以确认后的位图为母稿生成，没有进行矢量重绘。

发行包仍不包含 ONNX 模型；客户端按需从 VisionGuard Server 下载模型。
