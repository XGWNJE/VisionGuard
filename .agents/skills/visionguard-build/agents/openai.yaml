interface:
  display_name: "VisionGuard Build"
  short_description: "Build and verify all six VisionGuard targets."
  default_prompt: "Use $visionguard-build to compile all VisionGuard targets and report pass/fail results."
policy:
  allow_implicit_invocation: true
