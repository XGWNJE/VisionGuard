interface:
  display_name: "VisionGuard E2E"
  short_description: "Run current device and runtime verification."
  default_prompt: "Use $visionguard-e2e to choose the narrowest current runtime test and report evidence."
policy:
  allow_implicit_invocation: true
